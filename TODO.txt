FF7 Accessibility Mod — TODO / Minor Issues
============================================

These are low-priority items identified during development. Not bugs blocking
current functionality; noted here for a future cleanup pass.


[MAGICMODE] Magic menu modes inverted — SHIPPED v2.30.52 (2026-08-01), awaiting retest
------------------------------------------------------------------------
Diagnostic-led (probe build + 20s session; screen stayed 2, trio never
moved => not a gate bail). v2.30.48 had modes backwards: jump table
0x7144CC case 0 = CHARACTER pane, case 1 = SPELL GRID (its handler
does the index + MP-check OK press). Mod announced a spell at screen
open then skipped the real grid. Fixed + character pane now announces
(watches BOTH party-slot and CHARSEL_CURSOR, Limit-menu style).
⚠ 3rd sighting: 0..2 wrap + party-ids-skip loop = CHARACTER cycler,
never a row cursor — check before naming. DIAG STILL IN (debug-gated,
logs charsel too): REMOVE once the retest passes. VERIFY: character
names as you choose, then spell names + "battle only"; log shows
"MAGICM mode 0 -> 1". Then also re-verify L1/R1 (v2.30.51 confirmed
working by the user before this build overwrote it).

[FLIPCUT] Char-flip announce cut off — SHIPPED v2.30.51 (2026-08-01), CONFIRMED by user 2026-08-01
------------------------------------------------------------------------
L1/R1 spoke only the gear row: the name announce (interrupt) was cut
by the same-poll row re-announce (also interrupt). Fixed in ALL FOUR
flip branches (equip/limit/magic/materia) via the entry-path queue
flag. CLASS RULE (3rd sighting): one action → two utterances = second
QUEUES; review new threads' entry/flip branches for back-to-back
interrupt=true. VERIFY: flips speak name THEN row, both complete.

[EQUIPFIX] Equip cursor play-correction — SHIPPED v2.30.50 (2026-08-01), awaiting retest
------------------------------------------------------------------------
v2.30.34's 0xDCA4A4 "category cursor" was the L1/R1 CHARACTER cycler
(party-ids skip loop in the very wrap disasm that settled it — two
0..2 cursors, wrong one picked). Real row cursor = 0xDCA5C4 (OK
handler passes it to the candidate builder). Also fixed: gear names
now resolve via the screen's own slot, not CHARSEL_CHOSEN (pre-flip
character bug). VERIFY (the user's exact repro): choose Barret →
rows speak with his gear ("Weapon: Gatling Gun"); up/down voiced;
list tracks; L1/R1 flips announce. §4/§14 corrected.

[DEFEND] Defend/Change announced — SHIPPED v2.30.49 (2026-08-01), awaiting retest
------------------------------------------------------------------------
Defend is vanilla-hidden: Right on Attack = Defend, Left = Change (row
swap) — dedicated widget states 2/3, not table entries (disasm in
research §8 v2.30.49). BattleMenuThread speaks the labels on state
entry; cancel re-speaks the command. VERIFY: Attack + Right → "Defend";
+ Left → "Change"; OK executes; Cancel returns voiced. Also worth a
ReadMe/keys note next release: "On Attack, press Right for Defend."

[MAGICMENU] Magic menu connected — SHIPPED v2.30.48 (2026-08-01), awaiting play-test
------------------------------------------------------------------------
Last main-menu gap besides PHS. Static session (research §8 v2.30.48):
table[2]=0x710DFA; spell list = battle's per-slot list (0xDBA5A0 =
BATTLE_CHAR_BLOCK+0x108, stride 8); widget 0xDD1708 col/row/scroll;
slot 0xDD17E8; "battle only" = the exe's own gray table 0x714440
(read live — parity by construction). VERIFY: entry announce; names
match screen (7H retranslation included); grayed spells say "battle
only"; "Empty" cells; character switch (L1/R1?) re-announces; pane-1
"Use on whom?" + target names (LEAST-PROVEN — 0x921100/0xDD16D4;
if wrong, log MAGICM pane lines tell). RESIDUALS: I-key descriptions
(kernel2 magic-desc sig needed), MP cost + current MP, Summon and
Enemy Skill tabs (their widgets unmapped; watch logs when tabs used).

[K2SIG] 7th Heaven spell names — SHIPPED v2.30.47 (2026-08-01), awaiting play-test
------------------------------------------------------------------------
7H text mods rename spell tiers (Cure/Cura/Curaga) => the vanilla
"Cure|Cure2|" / "Attack|Magic|" heads never match => magic+command
sections unfound => battle magic list spoke bare "row N". Fix:
signature ladders with entry-count bands (magic 256 entries, command
24 — measured from vanilla kernel2.bin); matched sig remembered for
ValidatedSection re-checks; scan log appends msig=/csig=. VERIFY (7H):
spells speak by name in battle; log shows magic nonzero + msig='Cure|'.
If magic STAYS 0, entry 0 itself was renamed — log now proves it;
add a rung. NOTE: magic entries >=128 carry limit names (v2.30.35) —
ear-check a limit name on 7H too.

[HOTSPOT] elevtr1 elevator switch — SHIPPED v2.30.46 (2026-08-01), awaiting play-test
------------------------------------------------------------------------
The in-car switch is JESSIE'S SCRIPT (no line, no prop, no key opcode
— trace in research §8 v2.30.46): nothing existed for the browser to
list. New curated story-hotspot table (played-evidence-only rule)
emits "elevator switch, press OK" under Triggers in elevtr1 at
(-140,40,5), inside Jessie's talk radius. VERIFY: listed in the
elevator; route lands at the panel; OK speaks "Switch On.". RESIDUAL:
the hotspot lists on ALL elevtr1 visits (no story gating — deliberate;
revisit only if the always-on listing confuses). Future entries: add
to kStoryHotspots (proxy.cpp) with trace evidence, never guesses.

[FIELD3] System messages + taken items + switches — SHIPPED v2.30.45 (2026-07-31), awaiting play-test
------------------------------------------------------------------------
Three tester reports, one version (research §8 v2.30.45 has the full
derivations). VERIFY: (1) speak_dialog=false → pick-up messages speak,
story speech muted, wait tones unaffected; watch for MISCLASSIFIED
speaker dialogs sneaking through (log tags every decision). (2) Taken
items vanish from Items + stop chirping; ordinals stable ("Item 2"
keeps its name after "Item" is taken). (3) nmkin door props list as
", device" under Triggers; reactor elevator button speaks "elevator
button, scene". RESIDUALS: People NOT vis-filtered yet (collect vis=
log evidence of scene hide/show churn first — invisible NPCs are
still listed); SCENE-vs-device line reclassification rejected for
lack of an honest offline discriminator (buttons and cutscene lines
both reach MES) — revisit if more line-buttons get reported; device
naming leans on dev labels (only 'evb' hand-translated so far).

[TESTMODE] debug_log defaults ON — v2.30.44 (2026-07-31), REVERT WHEN TEST PHASE ENDS
------------------------------------------------------------------------
User decision: mod is a test mod until further notice; logging on by
default so tester reports carry evidence. Revert (one commit) when the
user calls the test phase over: config.h debug_log default, canonical
cfg list line + glossary text, log.h comment — grep "test period"
under AccessibilityMod/. Users can opt out live via F8 > Debug log.

[K2SCAN] Kernel2 scanner crash fix — SHIPPED v2.30.43 (2026-07-31), awaiting tester verify
------------------------------------------------------------------------
Tester crash at the scorpion boss (NATIVE Win11, i5/GTX1650, FFNx
stable — NOT the VM tester; environment corrected by user 2026-07-31,
see research §8): TOCTOU in
ScanKernel2Sections — region freed mid-memchr-sweep => AV on the
BattleMenuThread (FFNx dump symbolicated via new /MAP linker map;
research §8 v2.30.43 has the full derivation). Fix: SEH guard per
region sweep (AV => skip region, avs= counter in scan log) +
exponential backoff on fruitless scans (menu sites were re-sweeping
the whole address space every 3s on never-match installs) + urgent
bypass for battle call sites (COMMAND is re-found every battle).
VERIFY (tester): scorpion fight completes on v2.30.43; their log scan
line may legitimately show avs>0. VERIFY (local): battle command/
magic/item names still speak; scan line shows streak=0.

[F8MENU] In-game audio settings menu — SHIPPED v2.30.42 (2026-07-31), awaiting play-test
------------------------------------------------------------------------
F8 toggles a spoken settings menu (no graphics): J/L or [ ] move
through all 18 cfg settings, Shift+J/L or - = change (volume steps 10
with a demo tone), K repeats, I describes, F8 closes. Changes apply
live (all consumers re-read Config::Get() — audited; debug_log via new
Log::SetEnabled) and save to the cfg instantly (SaveSetting in-place
line rewrite, offline-verified surgical on the canonical cfg).
Pathfinder + shop/materia/equip I-keys stand down while open; naming
screen refuses the menu (J/L/K would type letters). VERIFY: full
checklist in research §8 v2.30.42 — especially: J/L wrap + position
speech, save-failure warning absent on normal installs, cfg updated
while game still running, pathfinder resumes after close, F8 refusal
speech on naming screen, mid-session debug_log off/on appends.
RESIDUALS (deliberate): no gamepad binding for the menu itself
(keyboard-only; stick still suspended while open); menu speech honors
interrupt=false queuing (user preference, may feel slow when browsing
fast — revisit only if reported).

[TONES] waveOut tone player — SHIPPED v2.30.41 (2026-07-31), awaiting tester verify
------------------------------------------------------------------------
Remote tester (Win11 ARM VM on Apple Silicon, 7th Heaven + Echo-S):
NO tones audible, but their log PROVED every tone path fired (WALL
body lines print inside the Beep-calling block) — kernel32 Beep() is
silent in their VM. All 7 tone sites now route through Tones::Play
(tones.cpp: waveOut sine on WAVE_MAPPER, blocking, 5ms fades, Beep()
fallback on any failure; winmm loaded from System32 by absolute path,
never linked). New cfg setting tone_volume (0-100, default 60, master
mute at 0) — in the canonical cfg list + glossary per the [CFG] rule.
VERIFY (tester): new build audible in the VM; log shows "Tone player:
waveOut ready". VERIFY (local): tones unchanged-but-cleaner, follow
the game's Volume Mixer slider, tone_volume=30 quieter. Watch for:
first-tone latency (per-call waveOutOpen on a slow driver), overlap
of simultaneous tones (now mixes instead of queuing).

[CFG] Config restructure + DLL-created default — SHIPPED v2.30.39 (2026-07-27)
------------------------------------------------------------------------
User request: all settings at the top of ffvii_accessibility.cfg one
per line, descriptions/examples in a glossary below; the DLL now
CREATES that canonical file when the cfg is missing (it never wrote
one before). Single source of truth: CMake embeds the repo cfg into
the DLL at build time (default_config.h byte array — the cfg is 16.5KB,
over MSVC's ~16K string-literal cap). Both installs redeployed in the
new format with live values kept (debug_log=true,
speak_enemy_hp_always=true). RULE for future settings: add the key to
the TOP list and its description to the GLOSSARY (commented # lines
ONLY — an uncommented key=value example in the glossary would override
the top list, parser is last-one-wins). VERIFY (quick): rename the cfg
away, launch, confirm a new-format default file appears.

[TITLE] Launch-splash false "New Game" — CONFIRMED 2026-07-27 12:16 launch log
------------------------------------------------------------------------
✓ CLOSED: the 12:16 launch log verified the disasm end-to-end —
state=0 through the logos (silence), state=1 exactly at the menu with
the cursor announce, state=2 on fade-out, Continue grid spoke.
Residual observation only: no state=-1 line logged after handoff to
the field (gate is ==1, unaffected). Same log caught the quit-cursor
stale "Yes" at menu open — fixed v2.30.40 (research §8), verify
passively: menu opens speak only the row, log line says "seeded".

Original issue (fixed v2.30.38): TITLE_CURSOR is BSS-zero pre-title
and the old gate was FIELD_ID==0 only, so "New Game" spoke during the
company-logo splash. TITLE_STATE 0xDD74E0 ==1 is the gate (research
§8 v2.30.38); the v2.30.37 game-over prompt detection rides it too.

[GAMEOVER] Game-over sequence accessibility — SHIPPED v2.30.37 (2026-07-27), awaiting play-test
------------------------------------------------------------------------
Play report (Screenshots/game_over/ + 10:02-10:03 log): party wipe →
"Cloud is down" → 40s of SILENCE over the GAME OVER reel → stale
"Yes"/"Item" spoken over the NEW GAME/Continue prompt. Root cause:
game over never clears FIELD_ID, inverting every title/field gate.
Fix: GameOverWatchThread latches on the transient GAME_MODE==26 blip
(the only positive signal; research §8 v2.30.37), speaks "Game over.",
hands the title threads the context, stands down the stale menu
family, and clears when the game reloads.
VERIFY on the next deliberate death: (1) "Game over." spoken as the
reel starts (log "GAMEOVER: mode=26 observed" — if a wipe shows NO
such line, the 30ms poll missed the blip: report, the battle-side
fallback is the planned fix); (2) NO "Yes"/"Item"/stale-menu speech
at the prompt; (3) the prompt speaks "Title screen. New Game" (or
Continue) once it's up, cursor tracks Up/Down — the separate-cursor
worry is SETTLED statically by v2.30.38 (same title module, same
widget, prompt detection now rides TITLE_STATE);
(4) Continue → save grid speaks slots like a cold boot; (5) loading
a save returns everything to normal (menus/nav/dialog all speak —
log "latch cleared"); (6) G on the reel/prompt is silent; (7) J/L/K
on the reel are silent. New Game path clears the latch too (intro
movies → field).

[REVIEW] v2.30.36 review-sweep fixes (2026-07-26), awaiting play-test
------------------------------------------------------------------------
Nine fixes from the multi-agent code review (research §8 v2.30.36 has
the full write-ups). VERIFY: (1) NO spurious wait chime right after
dismissing a dialog (the close re-fire); (2) voice-mod config
(speak_dialog/speak_choices off): short dialogs chime at the wait, ONE
double-tone per choice window (was machine-gunning); (3) multi-page
ASKs on counter windows don't re-speak a garbled lead-in mid-choice;
(4) "Tutorial finished." is actually HEARD before the menu re-announce
(~1.5s gap); (5) tutorial suppression survives the field→menu request
gap (no cursor chatter over the lesson start); (6) G speaks gil on the
WORLD MAP; title screen G stays silent on a fresh boot (stale-caption
G after quit-to-title = known cosmetic residual). Log markers:
"CLOSE committed", "PENDING tracked (speech off)", "choice page
reached (rel=N, positional|mirror|timeout)", "latch release deferred".
SKIPPED (deliberate): fieldbg unknowns stay MC_SCENERY — future
offline catalog pass to whitelist talk-scripted fieldbg variants.

[LIMIT] Limit menu — SHIPPED v2.30.35 (2026-07-26), awaiting play-test
------------------------------------------------------------------------
Dispatch index 7. Speaks: "Limit. Cloud. Limit level 1" entry;
Set/Check bar; grid moves as "Level 1: Braver, Cross-slash" /
"Level 4: not learned"; char page-flip re-announces. VERIFY: entry
line matches the header's limit level; bar labels; grid navigation in
BOTH Set and Check panes speaks levels+techniques; setting a level
works; ⚠ EAR-CHECK Tifa's and Aeris' screens specifically — their
technique names come from kernel blocks that SWAP their order vs the
savemap (kernel2-derived; wrong names for exactly those two = the
swap map needs inverting). Log: "[FF7Access] LIMIT mode" lines give
the real mode mapping. RESIDUALS: Check-mode per-technique sub-cursor
(if any) unmapped; limit descriptions unspoken (future magic-desc
section, sig "Restores HP|Restores HP|").

[EQUIP] Equip menu — SHIPPED v2.30.34 (2026-07-26), awaiting play-test
------------------------------------------------------------------------
Dispatch index 4 (FFNx menu_sub_705D16; table[15] dupe accepted).
Speaks: "Equip. Barret" entry; "Weapon: Gatling Gun" category rows
(re-announced when an equip commits under the parked cursor);
candidate list names; I = gear description; char page-flip
re-announces. VERIFY: entry once; Up/Down over Weapon/Armor/Accessory
rows; OK opens list ("[FF7Access] EQUIP pane 0 -> 1" in log) and rows
speak; equipping returns to rows and speaks the NEW gear; armor
descriptions are BLANK by game data (I says "No description" — not a
bug); accessory descriptions work. RESIDUALS: stat compare deltas
(Attack 14->16) unspoken (needs the computed-stats scratch); slot/
growth line unspoken; owned-count suffix unspoken.

[MATERIA] Materia menu — SHIPPED v2.30.33 (2026-07-26), awaiting play-test
------------------------------------------------------------------------
Dispatch index 3 (= the v2.31 "table[3]" mystery screen). Speaks:
"Materia. Cloud" on entry; Check/Arrange bar; "Weapon slot 3:
Lightning materia" slot navigation; equip list ("Choose materia.");
Arrange popup (Arrange/Exchange/Remove all/Trash); arrange lists;
equip-in-place announcements; I = description + AP/Mastered.
VERIFY: entry announce once (no stale-row talk-over); slot cursor
tracks incl. weapon<->armor row moves; equip flow: OK on slot ->
"Choose materia." + list rows -> OK -> back on slot with the NEW
materia spoken; Arrange popup rows; character page-flip re-announces
name; log "[FF7Access] MATERIA mode" lines — report any mode outside
{0,1,3,4,5,6,7,8,9,10} or announcements in the wrong pane.
RESIDUALS (v1 scope): no star-level/"to next level" (needs kernel
materia-data AP thresholds — future static session); linked slots and
slot counts unspoken; Exchange phase 2 narrated as plain list.

[SHOP+DIALOG] False "Save" on shop open + short-dialog chime hole — FIXED v2.30.32 (2026-07-26)
------------------------------------------------------------------------
(1) Shop open spoke "Save": stale MENU_CURSOR (row 9) + shop's
MENU_OPEN → MenuCursorThread announced over the greeting. All six
main-menu threads now stand down on foreign menu screens (name entry
6 / PHS 7 / shop 8) via MenuModuleForeignScreen. VERIFY: shop open
speaks greeting then "Buy, Sell, or Exit. Buy." with no stray menu
row; main menu itself still announces normally after a shop visit.
(2) Short single-box dialog had no chime: a message rendered in one
pointer jump failed the >=3-changes liveness test → blacklist muted
it. Liveness now also proven by pointer POSITION past the message
start. VERIFY: short one-box NPC lines (the kid) chime; log line
shows rel= on each tone. Research §8 v2.30.32.

[DIALOG] Wait tone muted on some pages — FIXED v2.30.31 (2026-07-26), awaiting play-test
------------------------------------------------------------------------
Player report: some dialogs don't chime at the continue wait, no
visible pattern, worst in long multi-block same-window dialogs. The
pattern was TEXT LENGTH: counter-style windows rest on a data-
dependent state value after typing each block; short blocks can settle
on 1 or 2 = the v2.30.6 "never fire" blacklist = that page's chime
permanently muted. Fix: typewriter-pointer stillness as a style-
independent wait signal (pointer proven live this dialog via >=3
changes, then still >=300ms) — overrides the blacklist and replaces
the bare state-hold fallback (which also used to chime mid-typing on
positional windows). Research §8 v2.30.31. VERIFY: the previously
silent blocks now chime (replay a known offender if one is memorable);
no chime mid-typing; no double-chimes; log line now shows
"ptrstill=/chg=" — if any block STILL doesn't chime, that line (or its
absence) names the failing path.

[BATTLE] H key: current character HP/MP/status — SHIPPED v2.30.30 (2026-07-26), awaiting play-test
------------------------------------------------------------------------
FF4-parity H key (battle only): "Cloud, HP 244 of 302, MP 54 of 54.
No status effects" — or the status list (Poison, Fury, Barrier...).
Character = BATTLE_ACTIVE_SLOT (whose menu is open / last acted;
leader before the first turn). Statuses = actor_vars statusMask via
the standard kernel bit table (research §8 v2.30.30). VERIFY: H mid-
battle names the right character at the right moment (during another
ally's turn it should name THAT ally); numbers match the screen;
poison/fury/barrier callouts match reality (debug log prints the raw
mask — compare if anything sounds wrong); H does nothing outside
battle; G still works.

[TUTORIAL] Per-slide narration — SHIPPED v2.30.29 (2026-07-26), awaiting play-test
------------------------------------------------------------------------
Play report on v2.30.27: whole lesson read as one buffer, then silence
while guessing how slides advance. Root cause mapped statically
(research §8 v2.30.29): slides advance on ANY key (the window-close
test is literally pressed!=0); the confusing part was the demo phases
between slides, where the tutorial VM INJECTS key presses and discards
real input. Now: each slide speaks when its window shows ("Tutorial.
<first slide>"), then "Press any button to continue."; "Tutorial
finished." when the VM ends; menu-thread suppression released on the
engine flag instead of menu close. Save-screen info popups (same
window renderer) now speak too. VERIFY on the next tutorial (Materia
tutorial replays from the Sector 5 save?): one slide per window, hint
after each, no long buffer, no silence at the end; menus speak
normally again right after "Tutorial finished."; no double-speak of
save popups. RESIDUAL: if a slide ever seems to need a SPECIFIC key
(not any key), capture the log — would mean a second window mode
beyond the two decoded.

[SHOP] Shop menus + G gil key — SHIPPED v2.30.28 (2026-07-26), awaiting play-test
------------------------------------------------------------------------
User request (with item-shop screenshots): connect the shop menus; add a
gil-announce key. Entirely static-derived in one offline session
(ff7_shop_static.py; research §8 v2.30.28, §4/§14 shop blocks): gate =
GAME_MODE==8, state machine 0x92565C (7 states), cursor widgets, catalog
0x923418, price table [0xDD4720]. Speaks: shop greeting + title on open;
Buy/Sell/Exit and Item/Materia bars; buy rows "name, price gil, own N"
(+ "You have N gil" entering the buy list); sell-item rows "name, N
owned, sells for M gil / can't sell"; sell-materia rows (AP price /
mastered = base×70); quantity screens "N, total M gil"; I key = ware
description (FF4 parity); G key = current gil anywhere in a loaded game
(suppressed on the naming screen — G is text input there).
VERIFY at the Sector 5/Wall Market shops: (1) greeting spoken once on
open, no double-speak with the field dialog engine (the shop dialog
boxes are MENU-module windows, invisible to MESSAGE hooks — if the
greeting ALSO arrives via a field dialog on some shop, expect a double
and report it); (2) bar cursors announce Buy/Sell/Exit correctly (order
= state map check); (3) buy list speaks correct names/prices vs the
sighted screen, cursor tracks rows incl. scrolling past 5-6 visible
rows; (4) sell item list: 2-column grid — LEFT/RIGHT column movement
must track (the col+(row+scroll)*2 formula is the least-proven piece);
(5) sell materia list prices match the "Price through AP"/"Price for
Master" panel; (6) quantity up/down speaks count+total, buy total =
price×qty, sell total = half; (7) I key reads descriptions (armor has
none by data — expected silence-with-"No description"); (8) G key in
field/menu/shop/battle; check debug log "[FF7Access] SHOP" lines for
any state value NOT in 0..6 (would mean an unmapped screen, e.g. a
buy-confirm popup — log it, don't guess). RESIDUALS accepted at ship:
equipped-count not spoken on buy rows (sighted header shows
Owned+Equipped; "own N" = inventory only for materia); weapon-shop
"try on" / stat-compare pane unmapped (separate screen, likely its own
state value — harvest from the debug log when a weapon shop is
visited); accessory descriptions rely on a raw-byte signature
(0xB2 colour code head) — if the scan log shows acc_desc=00000000,
that signature needs re-derivation from a live dump.

[TIMER] Stale timer resurrects itself on a save loaded past the escape — FIXED v2.30.8 (2026-07-20)
------------------------------------------------------------------------
Player report: "When I load the game from the slums, the timer from
the escape starts again immediately. It should be cleared or
something." Confirmed by the session log: the No.1 Reactor escape
finished with ~596 seconds still on the clock; FF7 never resets that
savemap field afterward, so it just keeps ticking down once per real
second forever, invisible in vanilla play (its clock window closed
with the escape), but SAVED to every subsequent save file. On a fresh
launch loading such a save, TimerThread saw a still-decrementing
nonzero value and announced "Timer started" — technically correct
about the VALUE, wrong about the CONTEXT. Fix: live-hook STTIM (opcode
0x38, same execute_opcode_table pattern as MESSAGE/ASK) instead of
trusting the value alone — TimerThread now requires Hooks::SttimSeen()
(true only after a REAL STTIM call fires this process run) before
treating any nonzero value as a genuine timer; also makes T/Shift+T
correctly say "No active timer" while unarmed instead of reading the
stale number. RESIDUAL, unconfirmed either way: a save taken WHILE
mid-escape (a deliberate checkpoint) may or may not re-fire STTIM on
reload — if a play report shows that case going silent, add a second
arm signal for it rather than reverting this fix. Research §8 v2.30.8.
Deployed both installs. VERIFY: loading past a completed escape stays
silent; the next genuinely new timed sequence still announces normally.

[DIALOG] Wait/choice audio cues — SHIPPED v2.30.5, RETUNED v2.30.6 (2026-07-20), awaiting play-test
------------------------------------------------------------------------
User request: a short high tone when a story dialog is waiting for the
confirm button, a short high double-tone when a choice menu appears.
v2.30.5's "state unchanged this frame" rule false-fired: win=0's state
byte PARKS at value 2 for the whole typing duration (~900ms observed)
before jumping straight to a terminal value, so v2.30.5 fired almost
immediately (mid-typing, before the game would accept the button
press) and sometimes AGAIN when the real wait arrived — matching the
player reports "beeps multiple times" and "press 2-3 times before it
advances". v2.30.6: three-tier fix — confirmed terminal values {4,6,14}
fire after a short 80ms hold-confirm (long enough to reject win=1's
transient pass-through of 14); confirmed non-terminal {1,2} never fire
regardless of hold length; everything else falls to a 300ms debounce.
Research §8 v2.30.6. Deployed both installs. VERIFY: wait tone fires
once per hold, not early, not doubled, across both single-page and
multi-page dialogs; choice tone once per ASK menu, audibly distinct.

[BATTLE] Whose-turn announce on menu open — SHIPPED v2.37 (2026-07-19), PLAY-CONFIRMED 2026-07-20
------------------------------------------------------------------------
User request: name the character when their battle menu opens. No new
addresses (BATTLE_ACTIVE_SLOT + PartySlotLabel). "Cloud's turn. Attack."
folded into the first command announce (avoids clobber). Fires once per
turn session (command + submenus + targeting); cancelling among
submenus doesn't re-announce; a fresh turn for the same character does.
Research §8 v2.37. Gate: speak_battle_menu. PLAY-CONFIRMED 2026-07-20
("the character turn announcement has been play tested and works").

[BATTLE] List-select fix + scene-message reader — SHIPPED v2.36 (2026-07-19), PLAY-CONFIRMED 2026-07-20
------------------------------------------------------------------------
Two player reports fixed:
(1) items/magic mis-announce in battle. Confirm-path disasm proved the
    three lists differ: ITEM = 1-col stride-6 u16, empty 0xFFFF (the
    old code ALSO skipped id 0 = POTION → Potions silent; fixed).
    MAGIC/SUMMON = 3-COL grid, stride 8, u8 id, empty 0xFF, index =
    w0+(w4+scroll)*3 (v2.9's linear formula only matched a <=3-spell
    single row). List-cursor code now branches per list type.
(2) scorpion "tail's up" warning never spoke: enemy AI dialogue comes
    through the battle text display queue (0xBF1EB8), a channel nothing
    read. BattleMessageThread reads scene messages (buffer_idx >=0x100)
    from 0x9AD1E0 + offset table, dedups by buffer_idx, speaks queued.
    Shipped speculatively (scorpion one-shot) + debug-logged.
Research §8 v2.36. Gate: speak_battle. Deployed both installs
2026-07-19 (hash-verified). PLAY-CONFIRMED 2026-07-20 ("the battle
item/magic list has been play tested and appears to work" — fix (1)
confirmed; (2) not separately reported on, no missing-warning report
either).

[BATTLE] Victory screens TTS — v2.35 CONFIRMED + v2.35.1 stale-row fix (2026-07-19)
------------------------------------------------------------------------
"Victory! Gained X experience and Y AP." on the results opening (pools
0x99E2C0/C4/C8 captured at entry — the game consumes them on apply);
"<name> grew to level N!" from the savemap level watcher; "Gained X
gil, total Y." + drop item names / "No items" on the gil screen (mode
byte 0xDC1300, FFNx-branded phases). Research §8 v2.35. Gate:
speak_battle. PLAY-CONFIRMED 2026-07-19 ("It works") with one
regression fixed same day (v2.35.1): the victory screens raised
MENU_OPEN and the main-menu open-re-announce spoke the stale row
("Item"/"Config") over the victory line — suppressed via
g_last_battle_tick (menu opening within 4s of GAME_MODE==2 = results
context) + g_victory_active (VictoryThread's live state); same guard
added to the Item/Status dispatch gates (latent same-shape bug).
v2.35.2 (same day, play report "announcements trail the flow"): the
mode byte advances on OK PRESSES, not screen appearances (0=EXP screen
showing, 1=roll-up/chirps, 2=gil screen showing, 3=after its OK —
mode 2's meaning now settled by the player's description). Victory
line re-timed to the RESULTS WINDOW OPENING (battle-recency signal,
pools captured intact at mode 0); gil/items line to mode-2 entry
(fallback 3); gil total read there is pre-apply = what the screen
shows. STILL VERIFY ON THE NEXT FEW WINS (debug_log still true):
  - victory line lands BEFORE the first OK/chirps; gil line when the
    gil screen appears;
  - numbers match the visible screens; level-up line fires;
  - drops speak correct names; log "VICTORY drop[i] id=N f4=M" names
    the f4 field (qty? taken-flag?) from real data.

[DIALOG] Long-dialog paging: silent after page 1 — FIXED v2.30.7 (2026-07-20)
------------------------------------------------------------------------
Player report chain: (1) long dialogs re-read parts when pressing to
continue [v2.30.3, superseded]; (2) fixed pacing but spoke the WHOLE
message at once [v2.30.4's DecodePages, page-break-bytes-only split];
(3) v2.30.4 fixed dialogs that HAVE explicit page-break bytes, but for
dialogs that soft-wrap to a new screen with NO such byte (confirmed by
the state machine's own PAGE transitions still firing), DecodePages()
returned just 1 page — spoke it all at once, then dead silence with no
beep for every later on-screen page (2026-07-20 follow-up: "it also
does not split the dialog up... speaks the entire dialog in one pass").
v2.30.7: DecodePages() retired, replaced by FF7Text::DecodeMessagePages
— groups decoded lines into ~4-line pages (FF7's documented box
capacity, an ASSUMPTION not read from the game) but still force-closes
early on an explicit page-break byte when present. Research §8 v2.30.7.
Deployed both installs. VERIFY: dialogs speak one beat per real
on-screen page turn, including ones with no explicit page-break byte.

[DIALOG] Word-boundary gluing around tokens/control bytes — FIXED v2.30.4, comma decoded v2.30.6 (2026-07-20)
------------------------------------------------------------------------
Player report (Wedge line, train graveyard): "Hey[item name]What about
our money?" and "Uhnothin'.sorry." — words fused with no space. Cause:
dynamic-token/inline-name substitutions spliced text in with no
compensating space, and unhandled control bytes vanished with NONE at
all (unlike discarded wide chars, which already got one from the tail
filter). Fix: guard_space() called at every content-affecting branch in
decode_walk (ff7_text.cpp), guarding leading space before insertions/
skips and trailing space after token/name substitutions. Research §8
v2.30.4. Player follow-up 2026-07-20 confirmed spacing fixed ("Hey
[item name] What about our money?" — now has spaces) but flagged the
CONTENT as still wrong ("confusing '[item name]' with someone else's
name"). v2.30.6: cross-checked ~20 raw dumps from the session log and
found 0xE2 decodes as a COMMA in every one (no source table names it —
live-corpus finding) — it was falling into the generic unknown-byte
gap. The Wedge line reads correctly as "Hey, [???], what about our
money?" once restored. `decode_walk` now emits ',' for 0xE2 directly.
STILL UNRESOLVED (deferred, see next item): the "[item name]"
placeholder itself is very likely the WRONG type (almost certainly
Barret's name, not an item — "Hey, Barret, what about our money?" is
the well-known scene).

[DIALOG] "Dynamic tokens" 0xEB-0xF0 — RESOLVED v2.30.17 (2026-07-23): they're just character names
------------------------------------------------------------------------
CLOSED, and much simpler than the deferred investigation assumed. The
0xEB-0xF0 mid-string bytes were being decoded as 4-byte "dynamic
tokens" (header + 3 data bytes, spoken as "[item name]"/"[number]"
placeholders) per an FFNx-derived reading — while 0xEA (Cloud) and
0xF1/0xF2 (Vincent/Cid) were ALREADY single-byte name references. Two
independent raw captures with zero counterexamples proved the split
wrong for field dialog:
  - Tifa, 7th Heaven (2026-07-23 log): `57 49 54 48 00 EB 1F B3 E7 E0`
    = "WITH " {Barret} '?' '"' newline newline. The 4-byte reading
    CONSUMED the '?', the closing quote, and a newline as "data" —
    truncating the line AND speaking "[item name]" (player heard
    "item 1") for what is simply Barret's name.
  - Wedge, train graveyard (2026-07-20): "Hey [item name] What about
    our money?" = "Hey, {Barret}..." (the v2.30.6 residual already
    suspected this).
FIX: mid-string 0xEA-0xF2 is now ONE contiguous single-byte
character-name branch (ff7tk eng[] table: CLOUD..CID), live savemap
names respected. token_placeholder() deleted. No variable-bank
investigation needed. If some other text truly embeds a multi-byte
token in this range, its data bytes will now decode as VISIBLE garbage
in the debug log (re-investigate with evidence) instead of silently
eating real text.
STILL OPEN from the old entry: the `08 0A 3E 05 01` byte run (before a
page break, 2026-07-20 log) that decoded as garbage under the
"<=0x5E is ASCII" assumption — unexplained, unrelated to the name
range, needs its own capture if it recurs audibly.

[DIALOG] Choice menus: cursor tracking — REAL ROOT CAUSE FOUND + FIXED v2.30.10 (2026-07-21), awaiting play-test
------------------------------------------------------------------------
Player report chain: (1) only first option read, no cursor tracking
[v1]; (2) v2.30.4 wired ASKMENU_OPTION + DecodeLines but ASSUMED cursor
starts at option 0 — wrong, "spoken at the end of the choice set
instead of the beginning"; (3) v2.30.6 read the ASK opcode's own
FIRST_LINE/LAST_LINE params (disasm-confirmed: field_opcode_ask_
update_loop_6310A1 clamps the cursor to them) and seeded
ask_last_option from FIRST_LINE instead of 0 — player follow-up
2026-07-20 ("Aeris" flower-girl choice): SAME SYMPTOM PERSISTS ("first
choice spoken at the bottom... arrow back up to it, not spoken at
all"), even though the session log proves first_line/last_line read
correctly (2/3 for a 2-option choice) AND the cursor-change detection
fires for both directions (2->3, 3->2 both logged).
2026-07-21 (first pass): player supplied screen-by-screen CAPTURES of the
exact Aeris scene (Screenshots/Dialogs/flower_girl/). Misread at the
time as proof the index math was fine (the intro/quote/option layout in
the screenshots visually matched first_line=2/last_line=3) — v2.30.9
shipped a fix for a real but SEPARATE bug (ask_last_option was seeded to
the starting option specifically to suppress the OPTION CURSOR block's
first announce, which meant the default-highlighted option never got a
clean callout, only a buried mid-sentence mention inside the "Choose:"
intro). That fix was correct and still needed, but player re-test SAME
DAY showed the SAME symptom persisting -- v2.30.9 alone wasn't enough.
2026-07-21 (second pass, real root cause): a fresh debug_log=true replay
of the SAME scene (not just screenshots) gave the actual raw byte dump,
and it changes the diagnosis completely. The Aeris ASK raw bytes are
literally "Flower girl" E7 '"What happened?"' E7 E0 "You'd better get
out of here" E7 E0 "Nothing...hey...", and the "Buy one"/"Forget it"
window is E0 "Buy one" E7 E0 "Forget it". decode_walk (ff7_text.cpp)
was treating BOTH 0xE0 and 0xE7 as independent newline bytes that always
end the current segment -- so an E7 immediately followed by an E0 (or a
bare leading E0) produced TWO breaks with nothing between them, i.e. a
SPURIOUS EMPTY ENTRY inserted before every real option. Logged proof
(ASK[]  dump): Aeris case decoded to 6 entries -- 'Flower girl' /
'"What happened?"' / '' / "You'd better get out of here" / '' /
'Nothing.hey.' -- with the real options landing at [3] and [5], one
slot AFTER the blank artifacts. But the game's own FIRST_LINE/LAST_LINE
count REAL CONTENT lines (4 total: name, quote, option A, option B), not
raw break bytes -- so first_line=2 pointed at OUR blank filler, not
option A, silently no-op'ing the very first announce (line.empty()
check just skipped it) and shifting every subsequent cursor move's
announce one option behind the game's actual selection. This -- not the
v2.30.9 suppression bug -- is what reproduced "first choice spoken at
the bottom [i.e. never cleanly, only mid-intro]... not spoken at all
[the blank-entry no-op]" even after v2.30.9 shipped.
FIX v2.30.10 (ff7_text.cpp decode_walk): a newline-class byte (0xE0 or
0xE7) now only ends the current segment when that segment already has
real content; two in a row (or a leading one with nothing before it)
collapse into a single break instead of producing an empty entry.
Verified OFFLINE first (per this project's dry-run practice) against
BOTH captured raw dumps before redeploying: the fix reproduces the
game's own line count exactly -- 6->4 entries for Aeris (first_line=2
now correctly indexes "You'd better get out of here", last_line=3
"Nothing...hey..."), 4->2 for Buy one/Forget it (first_line=0/last_line=1
now index each option directly, no leading blank).
PLAY-TEST 2026-07-21 (evening session, log 20:13-20:14): v2.30.10
CONFIRMED for choices 1 and 3 of the Aeris scene — correct line counts
(4 and 2, double-break collapse working), correct option text at
first_line/last_line, starting option spoken, cursor tracked both ways.
The "nested choice silent" mystery was ALSO solved by the same log —
see the next entry (cross-field dialog_id collision, fixed v2.30.11).
Research §8 v2.30.6/.7/.9/.10. Deployed both installs.

[DIALOG] Second Aeris choice silent (was "nested choice tree silent") — ROOT CAUSE FOUND + FIXED v2.30.11 (2026-07-21), awaiting play-test
------------------------------------------------------------------------
Player report (same session that confirmed v2.30.10): choices 1 and 3
of the Aeris scene spoke perfectly, but choice 2 ("Don't see many
flowers around here" / "Never mind", fg_3_t2.jpg) said NOTHING — no
intro, no tone, no cursor announces. The session log captured it
exactly: at 20:14:01 an ASK opened on win=0 and ran its ENTIRE state
machine (START -> ... -> CLOSE all logged) with NO "[DLGID] pending"
line — meaning is_new_ask_dialog never fired because the opcode's
dialog_id EQUALED s_window[0].last_dialog_id. The stale value: the last
win=0 dialog before entering the flower-girl field was MSG id=3 on a
PREVIOUS field (20:12:15). dialog_id is FIELD-LOCAL — the flower-girl
field's second choice evidently also carries id=3 (neighbors are ids
0/1/5/6), colliding with the leftover. Everything downstream gates on
that one detector: intro speech, dialog_choice_tone, ask_lines
population (whose emptiness also no-ops the cursor block) — hence
TOTAL silence, matching the "silent including the intro" severity the
old nested-tree entry described. NOT actually nested-tree-specific:
any MESSAGE or ASK on any field whose id collides with the same
window's last id from a previous field would go equally silent.
FIX v2.30.11 (hooks.cpp): reset_windows_if_field_changed() — clears
every s_window[] slot's dialog tracking (last_dialog_id -> 0xFF
sentinel, pending/pages/ask state) whenever FIELD_FILE_BUFFER's pointer
changes (the same new-buffer-per-field signal
log_field_header_if_changed already relies on). Called at the top of
BOTH hook_message and hook_ask, so the reset lands on the new field's
first dialog opcode, before its dialog_id comparison. Safe by
construction: that first opcode call means no dialog predates it on
the new field. Logs "FIELD changed: window dialog tracking reset."
PLAY-CONFIRMED 2026-07-22: all of Aeris's dialog trees now function
correctly.

[DIALOG] Soldier fight-or-flee choice silent — ROOT CAUSE FOUND + FIXED v2.30.12 (2026-07-22), awaiting play-test
------------------------------------------------------------------------
Player report (same session that confirmed v2.30.11): the fight-or-flee
choices when soldiers confront you after the Aeris meeting don't speak.
Log evidence (09:29:32 pre-battle + 09:30:11 post-battle): decode was
PERFECT (ASK[2]='Fight them!', ASK[3]='Later!', first_line=2/
last_line=3) but the cursor read "option -1->0" — announcing the
'Cloud:' name line — then NEVER changed for the whole choice, both
times, while the same session's Aeris choices (same window id!) had
tracked flawlessly.
ROOT CAUSE: ASKMENU_OPTION (0xCC14D1) was never a cursor global. The
ASK handler disasm (already captured 2026-07-19) shows the live cursor
is a STACK LOCAL synced from/to the ASK opcode's OWN bank/address
script variable — per DIALOG. The live scan that found 0xCC14D1 simply
found the scanned dialog's variable; Aeris-field dialogs share it,
the soldier field's dialog doesn't (stale, frozen). A scan can never
tell a true global from one dialog's output variable — only
cross-field play exposes it.
FIX v2.30.12: read the per-WINDOW cursor mirror instead — the update
loop writes u16[0xCFF5DE + win*0x30] = line*16+6 (highlight pixel Y)
EVERY accepting frame (disasm 0x631384-0x63139C, in the 2026-07-20
ask_lines log all along). New ASK_CURSOR_PIXEL_Y + get_ask_cursor_line()
in ff7_addresses.h (validity: y>=6, 16px boundary, else skip frame);
hook_ask switched to it; ASKMENU_OPTION demoted to provenance-only in
§4/§14. Research §8 v2.30.12.
PLAY-CONFIRMED 2026-07-22 (second session): "All the dialog choices
that I encountered spoke properly now" — cursor source works
everywhere. Two follow-up polish items became v2.30.13 (next entry).

[DIALOG] ASK polish: stale-mirror double-speak + question/choice pause — FIXED v2.30.13 (2026-07-22), awaiting play-test
------------------------------------------------------------------------
Player reports after v2.30.12 confirmed: (1) "during some dialog trees
it spoke one of the choices multiple times rather than each choice
once"; (2) requested an audible pause between Aeris's "What happened?"
and the first choice.
(1) ROOT CAUSE, log-diagnosed (10:01:39): the per-window pixel mirror
keeps the PREVIOUS ASK's resting position until the new window starts
accepting input (~0.5-1.3s in the logs). At the soldier choice's open
the mirror still held 3 (the option chosen in the previous win=2
choice), so 'Later!' was announced spuriously, then 'Fight them!' when
the game clamped the real cursor in — one choice heard twice. Windows
where the stale value fell OUT of the new ask_lines' bounds skipped it
silently, which is why only SOME trees showed it. FIX: capture the
mirror's raw value at the PENDING frame as ask_mirror_baseline; until
the first announce, a reading equal to that baseline is presumed stale
and skipped — UNLESS it equals first_line (correct regardless, since
the game clamps the cursor into [first,last]). Accepted residual: a
re-asked dialog whose script var preserved a mid-range cursor equal to
the stale baseline misses its initial announce (first keypress
recovers) — strictly better than speaking a wrong option.
(2) FIX, same restructure: the intro no longer reads the whole window.
It now speaks "Choose:" + only the CONTEXT lines (before first_line —
speaker name + question), and the first option announce QUEUES
(interrupt=false) instead of interrupting, so it plays after the intro
finishes — the TTS utterance boundary provides the requested pause.
Options are no longer read in the intro at all, so each choice is
heard exactly once: the highlighted one after the question, the rest
as the cursor reaches them. Subsequent cursor moves still interrupt
(snappy flipping). Research §8 v2.30.13. Deployed both installs.
PLAY-CONFIRMED 2026-07-22 ("Everything seems to work now") — the
entire ASK choice-menu chain (v2.30.9-.13) is now verified working.

[DIALOG] win=1 dialogs silenced/skipped + same-NPC re-talks silent — FIXED v2.30.15 (2026-07-23), awaiting play-test
------------------------------------------------------------------------
Player reports (2026-07-23, screenshots in Screenshots/Dialogs/unsorted/
+ 7th_heaven/): (1) some shop owners not read; (2) 7th Heaven
conversation choices don't speak; (3) button chime missing for some
dialogs. Log evidence separated these into TWO real bugs:
1. FALSE CLOSE on counter-style windows. win=1's dialog state byte is
   the DENSE COUNTER style (0->1->3->5->7->9...->rest, known since the
   v2.30.5 wait-tone work) — it PASSES THROUGH value 7 while climbing,
   and the close detector ("current==7") fired there: TTS::Silence()
   cut win=1 speech off ~270ms in (the 7th Heaven MESSAGE audibly
   truncated) and wiped ask_lines, muting the whole 7th Heaven choice
   (decode was perfect — 'Yeah'/'Not this time' logged — but the
   cursor block no-ops with empty ask_lines). FIX: CLOSE commits only
   on the SECOND consecutive frame at 7 (per-window close_handled
   latch; a counter never repeats a value, discrete windows park at 7
   through the whole close animation — their close just lands ~33ms
   later). Guarded !pending_speak for the new-dialog-during-7-hold race.
2. SAME-ID RE-TALKS NEVER RE-SPOKE (any window). The Sector 5 shop
   owner's repeat dialog ran full state cycles with ZERO [DLGID] lines:
   re-talking to an NPC re-runs the same opcode with the SAME dialog_id,
   and last_dialog_id (deliberately kept = id after close to stop
   one-frame re-fires) made the detector permanently blind to it. FIX:
   from-idle RE-ARM — when a window's state leaves 0 (fresh open edge;
   win=0: 7->0 then 0->1, win=1: rest->0 then 0->1) and nothing is
   pending, last_dialog_id resets to 0xFF so the DLGID branch fires
   even for an identical id. LIMITATION: win=2/3-style windows whose
   state byte never moves produce no idle edge — same-id re-talks
   there remain undetectable (needs a different signal if reported).
3. Chime: could not be diagnosed — the wait tone was never logged.
   v2.30.15 logs every arm ("MSG win=N wait tone (state=X held=Yms)");
   next session's log will show armed-vs-inaudible per dialog. The
   win=1 false CLOSE also reset wait_tone_armed mid-dialog, so fix 1
   may cure some missing chimes by itself.
RESIDUAL to watch: whether win=1 ASK option-cursor announces work now
that ask_lines survives (the pixel mirror read at 0xCFF60E was invalid
at the PENDING frame — baseline=-1 — and no option lines appeared; if
they still don't after this fix, the win=1 mirror needs its own
investigation). Research §8 v2.30.15. Deployed both installs.
PARTIAL PLAY-TEST same day: win=1 re-talks CONFIRMED speaking (id=43
spoke on both talks, wait tones logged firing) — but exposed two new
issues fixed in v2.30.16 (next entry).

[DIALOG] win=2/3-style re-talks still silent + from-idle re-arm double-speak — FIXED v2.30.16 (2026-07-23), awaiting play-test
------------------------------------------------------------------------
Player report after v2.30.15: "Some repeating text isn't being spoken
again. The last conversation I had didn't repeat." Log showed:
1. The unrepeated conversation was on win=3 — the "state byte never
   moves" style (the limitation the v2.30.15 entry predicted): valid
   text on screen (wait tone armed, logged with held=169s), same id,
   ZERO state transitions — no idle edge for the state re-arm to see.
2. BONUS find in the same log: the v2.30.15 from-idle re-arm could
   DOUBLE-SPEAK — a window whose state byte is still parked at 0 when
   the dialog opens completes DLGID+PENDING (speaks) BEFORE the 0->1
   edge arrives; the edge then re-armed and re-spoke the same dialog
   200ms later (win=1 id=43, 10:58:16.146 + .345, both [PENDING]
   speaking lines logged).
FIX v2.30.16: rearm_if_reopened_after_gap() — the OPCODE-CALL-GAP
detector, now the primary reopen signal for both hooks. The
MESSAGE/ASK opcode runs every frame while its dialog is on screen and
not at all in between, so the first hook call for a window after
>500ms of silence IS a fresh dialog — works for EVERY window style
including never-moving-state win=2/3, and structurally cannot
double-fire (the gap exists only once per reopen; every later call
refreshes last_hook_tick). Logs "MSG/ASK win=N reopened after Xms gap
(re-armed)". The v2.30.15 from-idle state re-arm stays as backup,
now guarded by last_speak_tick age >1500ms (kills the double-speak).
Special windows (WSPCL clock etc.) run continuously, so they present
no gap and cannot spuriously re-arm. Known trade-off, judged a
feature: a dialog interrupted mid-display by something that freezes
the field >500ms (battle transition) re-speaks on resume — context
restoration for a blind player. Research §8 v2.30.16. Deployed both
installs. PARTIAL PLAY-TEST same day: gap re-arm CONFIRMED working
(win=3 re-talks spoke, logged "reopened after Xms gap"); win=1 ASK
option cursor CONFIRMED tracking (the v2.30.15 residual is RESOLVED —
option 2->1 transitions logged at the 7th Heaven choice). But the
7th Heaven scene exposed a truncation bug in the state re-arm — fixed
v2.30.17 (next entry).

[DIALOG] win=1 multi-page dialogs: first chars cut off per page — FIXED v2.30.17 (2026-07-23), awaiting play-test
------------------------------------------------------------------------
Player report + screenshot (dialog_1.jpg): '"I was worried."' spoken
as "was worried" in the 7th Heaven scene. Log caught the full
mechanism on a 3-page Tifa dialog: counter-style windows (win=1)
re-run their ENTIRE state cycle per PAGE (rest->0->climb->rest, same
id, never any 14->2/4->8 PAGE transition). The v2.30.15/16 from-idle
re-arm misread each page rerun as a fresh dialog and re-decoded the
rawptr — but the rawptr is the live TYPEWRITER pointer, and on a page
rerun the game starts consuming it immediately (no window-open grace),
so the deferred PENDING read lost the first ~3 chars: log shows
'"I should have known."' (page 1, complete — open race won), then
''s always pushing...' (lost '"He'), then 'was worried."' (lost '"I ').
FIX: the from-idle edge now branches on whether cached unspoken pages
remain: YES -> speak the NEXT CACHED page (the first decode already
holds every page complete, v2.30.4) — proper page pacing for
counter-style windows, no re-decode, no truncation; guarded >600ms
since last speak to skip the dialog's own opening 0->1 edge (which
arrives ~170ms after the PENDING speak and was the v2.30.16
double-speak). NO -> the old re-talk re-arm (>1500ms guard), kept as
backup for never-gapping windows. Research §8 v2.30.17. Deployed both
installs. PLAY-CONFIRMED 2026-07-23 ("The dialog fixes are confirmed")
— together with the Barret-name decode fix in the same build.

[NAV] Scenery props listed as unreachable "People" — FIXED v2.30.18 (2026-07-23), awaiting play-test
------------------------------------------------------------------------
Player report (7th Heaven bar): People category listed "swordc,
fieldbg hana, fieldbg cash, fieldbg pinbl, camera, and marine" — all
with no walkable path, none of them actual people (except one — see
below). Root cause: ClassifyModelLabel's fallthrough. A "fieldbg"
label matching none of the known interactable substrings
(trb/mtra/potion/sparkle/key/saveicn) fell through to MC_PERSON — but
the offline flevel catalog shows dozens of such labels game-wide
(doors "…dr", the train "kisya", "cos"/"props"/"v2"/"zuta"…): they are
background scenery (hana = flower vase, cash = register, pinbl =
pinball machine — whose ELEVATOR interaction is a separate line
trigger in the Triggers category; the model is just its picture).
FIX: new MC_SCENERY class — unknown "fieldbg" labels plus a
tight, evidence-only list of non-fieldbg props ("camera" exact,
"swordc*" prefix; 1 and 3 occurrences game-wide respectively) —
excluded from EVERY category including All (a sighted player doesn't
navigate to the cash register; unreachable-prop entries are pure list
noise). The proximity chirp is unaffected (it keys on talk radius,
which props lack).
AND: "marine" IS a person — Marlene (JP name "Marin"), standing
behind the bar counter (off the walkmesh, hence no path — correct).
Exactly 1 model + 2 entity occurrences game-wide, all the 7th Heaven
bar. Added marine/marin -> "Marlene" to the dev-name translation
table, so she now announces by name.
VERIFY: 7th Heaven People category lists only actual people (Tifa,
Barret, Marlene by name, patrons); no scenery anywhere including All;
other fields' People lists unchanged except scenery disappearing.
FOLLOW-UP v2.30.19 (same day): the hideout log showed the camera/swordc
exact-match MISSED — real labels carry location prefixes ("nible
camera", "modify swordc") — switched to contains-match (both tokens
unique in the catalog). "sub marine" confirmed translating via the
word pass (sub dropped, marine->Marlene).

[NAV] Ghost "people" from parked models + win=3 multi-page silence — FIXED v2.30.19 (2026-07-23), awaiting play-test
------------------------------------------------------------------------
Player report: pathfinder "a little buggy" in the 7th Heaven hideout +
more unspoken dialog (3 screenshots — all pages 2+ of multi-page win=3
dialogs).
1. GHOST MODELS: the hideout log showed models the scene hasn't placed
   yet parked at EXACTLY (0,0) with triangle id 0 — Tifa (upstairs at
   the time), the camera dummy, an ungrant­ed materia — passing the
   off-mesh filter (tri<0) because 0 is a VALID triangle id, and
   listing as unreachable ghosts. Visible models can also read tri=0
   but always have nonzero positions, so the filter is the COMBINATION:
   tri==0 AND pos==(0,0) -> skip. Dynamic: the scan re-runs, so a model
   the script places later appears normally.
2. WIN=3 MULTI-PAGE DIALOGS SILENT AFTER PAGE 1: win=3 is the
   never-moving-state style — no 14->2/4->8 PAGE transitions (win=0's
   signal), no state-cycle reruns (win=1's signal): page 1 spoke,
   pages 2..N never (log: Barret's 4-page "Yeah, you're strong."
   speech + the President's 2-page TV broadcast, exactly the
   screenshots). FIX: POSITIONAL page advance — DecodeMessagePages now
   also returns each page's raw byte offset; hook_message captures the
   rawptr at decode and, for state-static windows only (any observed
   state transition stands the mechanism down, so it can never
   double-fire against the state-driven paths), watches the live
   TYPEWRITER pointer's offset: entering page N's byte range (+2 slack
   for the page-break-boundary park) = the display turned to page N ->
   speak it from cache (never re-decodes — no typewriter race), re-arm
   the wait tone with a fresh debounce (win=3 multi-pagers previously
   chimed only on page 1). Logs "MSG win=N positional PAGE x/y
   (rel=...)". ASSUMPTION to verify in the log: win=3's rawptr advances
   like win=1's (v1 doc + win=1 evidence say yes); if the positional
   lines never appear, the win=3 pointer is static and this needs a
   different signal.
VERIFY: hideout replay — Barret's 4-page speech and the TV broadcast
speak every page; People list has no ghosts (Tifa only when actually
present); camera/swordc gone; wait tone per page on win=3 dialogs.
LOG-CONFIRMED 2026-07-25 (morning session): "positional PAGE" lines
fire throughout — win=3 4-pager spoke 2/4, 3/4, 4/4 (09:51:41-46,
Barret's speech) plus many 2/2 pages on win=2 and win=3. The
ASSUMPTION is settled: win=3's rawptr advances like win=1's. Ghost/
scenery filtering not separately reported on — no complaint either.

[DIALOG] Multi-page ASK: wrong "choices", silent caption, early tone — FIXED v2.30.20 (2026-07-23), awaiting play-test
------------------------------------------------------------------------
Player report (Tifa's drink offer, bar): tone flagged the lead-in page
as a choice; the "choices" spoke as "How about…" / wrong text; the
caption ("…something to drink?") never spoke. Log: the ASK raw
contains a PAGE BREAK (0xE8) — page 1 = 'Tifa: "How about."'
(lead-in only), page 2 = caption + the two real options. The game's
FIRST_LINE/LAST_LINE (1/2 here) index lines OF THE DISPLAYED PAGE;
the old code indexed them into the flattened whole-text line list
(['Tifa:', '"How about."', '".something to drink?"', "I don't feel
like it", 'Give me something hard'] — announced [1] and [2]).
FIX: hook_ask scans for the LAST page-break byte; ask_lines is now
decoded from the choice page only (page-relative, matching both the
opcode params AND the pixel mirror, which is inherently
page-relative); lead-in pages speak as plain narrative at open (no
"Choose:", no tone); the choice page's arrival is detected
POSITIONALLY (typewriter pointer entering its byte range, same
mechanism as v2.30.19) and fires the tone + "Choose:" + caption + the
queued highlighted option, gating the cursor block until then.
Single-page ASKs unchanged (tone moved from the DLGID frame to the
PENDING frame — one frame, imperceptible); speak_choices=off keeps
the old at-open tone (no decode available to know better).
VERIFY: Tifa drink offer — lead-in speaks at open, tone+caption+
"I don't feel like it" at the page turn, cursor tracks both options;
single-page choices (soldiers, Aeris) unchanged.
LOG-CONFIRMED 2026-07-25: ASK win=2 id=29 "[PENDING] multi-page:
choice page at +15, speaking lead-in" -> "choice page reached
(rel=18)" -> options queued and cursor tracked both directions
(09:49:17-25). Single-page ASKs (win=1 id=4, 09:47:36) also clean.

[MENU] Materia tutorial: text unspoken, cursor chatter — FIXED v2.30.27 (2026-07-26), awaiting play-test
------------------------------------------------------------------------
Player report: "when Cloud started the Materia tutorial things got
slow and choppy. Text boxes explaining the process were probably
firing but not spoken... the cursor was jumping around." Menu
tutorials are a SEPARATE script system (MENU module byte-code) —
invisible to the MESSAGE/ASK hooks — and the tutorial script drives
the menu cursor itself (the log's Item->Order sweeps at ~300ms/step,
each spoken with interrupt=true: the chatter/choppiness).
FIX v2.30.27: TUTOR opcode (0x21) hooked (same table pattern as
MESSAGE); the tutorial script is parsed straight out of the field
file's akao-offsets table (format offline-validated: 0x12 window,
0x10 text until 0xFF; ff7_tutorial_dump.py — 8 fields, 40 tutorials
game-wide) and the FULL LESSON is queued as one narration ("Tutorial.
Sure, I'll explain how Materia works." ...). While a tutorial runs
(TUTOR fired -> MENU_OPEN back to 0), the main-menu cursor, Order
focus, Item and Status announcements stand down; narration is
silenced if the menu closes early.
VERIFY: (a) replay the Materia tutorial (Barret re-asks? if not, the
next tutorial encountered) — the full lesson speaks, no cursor
chatter over it; (b) normal menu use unaffected after the tutorial;
(c) if "slow and choppy" PERSISTS even with speech quiet, report —
that would be a frame-rate issue to investigate separately, not
speech; (d) log shows "TUTOR opcode fired" + "N text window(s)".

[NAV] No proximity chirp near large-bodied NPCs (Barret) — FIXED v2.30.26 (2026-07-25), awaiting play-test
------------------------------------------------------------------------
Player report: "it doesn't always chime when a person I can talk to is
within range. For example, Barret stands outside the 7th Heaven bar...
It does not beep when I get near him." Arithmetic root cause: Barret's
talk radius (70) is SMALLER than the closest the player can get to his
center (48+32=80 body contact) — the v2.27 chirp condition
(center dist <= talk radius) is unsatisfiable for big bodies, yet
talking at contact demonstrably works (the same log shows it).
Static hunt (ff7_talk_range_static.py) found the talkR/tlkR2 handlers
(0x618253/0x6182DF: arg×scale>>9 -> +0x74) and the init defaults
(col=30·s>>9, talk=80·s>>9 — why "70" is everywhere at scale 448) but
NOT the engine's reader — so the shipped rule is the behaviorally
proven one: effective range = max(talk_radius, player_col + npc_col
+ 8). Chirp + pathfinder target_reach both use it. Intangible models
(col 0) keep the plain talk circle.
VERIFY: (a) walking up to Barret outside the bar chirps before/at the
bump; (b) normal NPCs chirp exactly as before (no new far-away
chirps); (c) if anything chirps at a clearly un-talkable distance,
report it — that would mean the engine adds only the NPC's radius,
not both (revisit with the reader hunt).

[NAV] Nonsense routes to Biggs: stale triangle hints + off-floor targets — FIXED v2.30.25 (2026-07-25), awaiting play-test
------------------------------------------------------------------------
Player report (evening): "Biggs: up and right 1 second, then left 2
seconds" from the bottom-left corner; then "Biggs: left 2 seconds.
Barret is in the way." — never reached him. Log smoking gun:
"NAV route start=0 goal=0 path=1 'left 2 seconds'" — Biggs' live
triangle field reads a stale 0 (scripted idle models never update
+0x78; same signature as the v2.30.19 parked-ghost finding), so every
route aimed at the wrong triangle and the funnel appended a fake
straight leg through walls (which genuinely passed near Barret —
hence the caution).
FIXES (offline-validated against the failing positions first):
  - ResolveTriHint: trust a model's triangle hint only if that
    triangle CONTAINS the model's position; else geometry-locate.
    Applied in the route builder AND journey planner.
  - Nearest-reachable routing: Biggs actually sits ON the crates
    (off-floor; his position locates to an unreachable sliver tri).
    NO_PATH now falls back (after the journey planner) to a real
    route to the nearest reachable point — "Biggs, off the walkable
    area: up and left 1 second." Stand point is 6u from him; talk
    radius covers it. Also upgrades Tifa-behind-counter class fields.
  - target_reach: body tests + cautions stop at the target's talk
    radius (live, floor 20 cap 90) — a companion standing beside the
    target no longer blocks or warns.
VERIFY: (a) Biggs route speaks a sane left-side route with no phantom
"Barret is in the way", ends in talk range (chirp) — talk to him;
(b) Jessie/Wedge routes sane (same stale-hint class); (c) counter/
crate NPCs elsewhere say "off the walkable area: <route>" instead of
"No walkable path found"; (d) routes to normal walking NPCs unchanged.

[NAV] Triggers spoke bare dev names — line-trigger behavior catalog SHIPPED v2.30.23 (2026-07-25), awaiting play-test
------------------------------------------------------------------------
Player report: stuck in the hideout wanting to go back upstairs, stood
on the 'border' trigger ("very close") — nothing happened. Script dump
(ff7_hideout_exit_script_dump.py) showed border2 = cutscene trigger,
and the real exit = the 'pinball' line (cross script -> REQSW cloud.s5
-> MAPJUMP 154, the lift; its [OK] script is empty — walk ACROSS it).
FIX: game-wide offline catalog (ff7_line_trigger_catalog.py; opcode
walker with cebix/ff7tools' canonical length/jump tables; 702 fields,
1,827 line entities, ZERO walk errors; validations vs the hand decode
+ nmkin_2 ladders + the street watchtower climbs all pass) generating
ff7_line_trigger_catalog.h; the Triggers category now appends what
each line DOES: ", exit to <place>" / ", exit" (conditional dest) /
", exit, press OK" / ", climb" / ", press OK" / ", scene" /
", inactive".
CAVEAT (by design): suffixes state what the scripts CONTAIN — an exit
can still be story-gated in the moment (its conditions unmet), and a
"scene" trigger only fires at its scripted story beats.
VERIFY: hideout Triggers list says "pinball, exit to <lift/bar name or
plain exit>", "border, scene", "TV, scene"; ladders in the reactor
fields say climb/ladder; no suffix nonsense on other fields; names
stay within the speech (nothing clipped).

[NAV] "Go left" walks into Tifa: solid NPC bodies block like walls — MITIGATED v2.30.22 (2026-07-25), rerouting DEFERRED pending radii
------------------------------------------------------------------------
Player report (7th Heaven hideout, 7h_hidout_1.jpg): "Pathfinder tells
me to get to Barret I need to go left 1 second then down and left 1
second, but if I try to go left I get the wall sound... facing the
right character is proving difficult."
DIAGNOSIS (fully offline, ff7_hideout_firstleg_dryrun.py, logs
20260725_10xx — the dry-run replica reproduced the live route EXACTLY,
path/corners/speech, IDLCK locks included, before any conclusion was
drawn):
  - The walkmesh route was CORRECT; the exact bearing walks clean.
  - The 8-way QUANTIZED "left" the player must hold passes through
    Tifa, who stood in the aisle doorway. The live log shows the
    player pinned at EXACTLY 64.0 units from her center for 3.5
    minutes: FF7 models hard-block each other (no body slide; walls
    do slide), 64 = 32+32 collision radii. A body is
    indistinguishable from a wall in every signal the mod reads.
  - Flood fill: at 64-unit contact the hideout's aisles seal
    completely (Barret unreachable) yet 56 opens them — per-model
    radii differ, and the room was playable sighted, so the real
    values are unknown. Arc-bypass detour prototypes produced zigzag
    corners that fold into fictional legs ("right 3 seconds") —
    quantized speech cannot express tight body slaloms.
SHIPPED v2.30.22 (the honest layer, no guessed physics in routing):
  - Wall-bump tone now speaks "<Name> is in the way." once per
    contact episode when a person stands within 90u / ±60° of the
    held direction — a body-bump no longer masquerades as a wall.
  - Directions announces (both turn-by-turn and straight-line) append
    the same caution when the first QUANTIZED leg's ray passes within
    56u of a person (destination excluded — routes to a person always
    end at their body).
  - NAV person debug line dumps rc6E/rc70/rc72 (the unmapped words
    between character_id +0x6C and talk_radius +0x74) — the collision
    radius lives in one of them; expect ~32 for people. WALL body:
    log line records each naming event with measured distance.
DEFERRED (needs the radii from the next session's log): body-aware
routing — temp-avoid triangles overlapped by body circles + re-A*
(the dry run's Experiment 1/3 shows the bottom-of-table route works
when radii are known), and only then a followable-legs validator.
Promote the confirmed radius offset to §4/§14 when the log lands.
VERIFY: (a) bumping into Tifa/any NPC says who it is; (b) the Barret
route says "...Tifa is in the way" while she stands in the aisle;
(c) log shows rc6E/rc70/rc72 values (report them); (d) no spurious
"in the way" cautions on clear routes.
RESOLVED SAME DAY — (a)/(b) play-confirmed ("works much better";
Barret and Jessie named in the log), and (c) needed NO extra session:
the 11:02-11:09 log already carried the dump. +0x72 = collision
radius (Tifa 30 / Barret 48 / trio 34; both contact anchors fit
player≈32 + npc; rc6E/rc70 rejected; DYNAMIC — scripts set it, 0 =
intangible). The deferred rerouting shipped as v2.30.24 (next entry).

[NAV] Body-aware rerouting — SHIPPED v2.30.24 (2026-07-25), awaiting play-test
------------------------------------------------------------------------
Built on the confirmed +0x72 collision radius (see previous entry).
Route legs are tested against every solid body's LIVE contact circle
(player_r + npc_r + 6); when blocked, triangles overlapped by the
blocking bodies' circles are temp-avoided (start/goal exempt, same
overlay shape as the IDLCK lock cut) and A* re-runs. The reroute is
adopted only if its funnel is clear of ALL bodies — otherwise the
old route + "X is in the way" caution stands (clear route or honest
truth, never a guess). Bump naming + route caution now use real
radii too (search = contact+26 in the ±60° cone; ray = contact+8).
Radius 0 (script-set intangible, e.g. Marlene reads 0) = no body.
KNOWN LIMIT (accepted): triangle granularity — a body in a large
doorway triangle seals that corridor even when a foot-width squeeze
exists (hideout Tifa in the top-strip doorway → falls back to the
caution; a sighted player shimmies that gap too).
VERIFY: (a) route to Barret with someone in the aisle either detours
("rerouted around N bodies" in the log, instructions differ from the
straight route) or says "X is in the way" — never silently walks
into a body; (b) clear routes unchanged, no spurious cautions;
(c) "col=" values in NAV person lines look sane across fields
(people ~24-48; 0 for parked/scene-intangible models);
(d) bump naming still fires (now radius-scaled).

[NAV] Route leads into a player-blocked area — ROOT CAUSE FOUND + FIXED v2.30.21 (2026-07-23), awaiting play-test
------------------------------------------------------------------------
Player report + screenshot (pf_1.jpg): route to Tifa walked the player
into the bar counter. INVESTIGATED AND FIXED same day — the hypothesis
held exactly: FF7's IDLCK opcode (0x6D) locks walkmesh triangles at
runtime, overlaying the static access pool the mod's A* reads.
Three static scripts (ff7_idlck_static/readers/bitmath.py, logs
20260723_2011xx-2021xx), no live scans needed:
- IDLCK handler 0x61E29F: u16 triangle + u8 flag -> set/clear bit
  (tri&7) of byte [0xCBF9D8]+0xB2+(tri>>3).
- The game's own movement edge-crossing code (0x6369E8/0x636AAF/
  0x636B76, one branch per edge) reads the neighbor from its parsed
  access pool (ptr 0xCFF748) and REFUSES the crossing when the same
  bit — at static 0xCC0E3A = MODULES_GLOBAL_OBJECT+0xB2 — is set.
  Polarity proven by the game's own branch: 1 = locked/impassable.
FIX: TRIANGLE_LOCK_BITS + is_triangle_locked() (ff7_addresses.h);
LoadWalkmesh() cuts every edge INTO a locked triangle (after the
reciprocity self-guard — locks legitimately make the graph
asymmetric). Locked-target routes (Tifa) now fail A* and fall back to
the straight-line + "no walkable path" announce, which walks the
player TO the counter where the talk radius reaches across. Debug
line: "NAV walkmesh: N edge(s) cut by triangle locks".
New §4/§14 rows: TRIANGLE_LOCK_BITS 0xCC0E3A, access-pool ptr
0xCFF748, handler + crossing-code addresses.
VERIFY: (a) Tifa route no longer walks into the counter (expect
straight-line "no walkable path" style announce or a route to the
counter front); (b) the debug line appears on fields with locks;
(c) previously "slightly off" routes in other fields (player hunch)
improve — report any that still thread walls.
PARTIAL LOG-CONFIRM 2026-07-25: (b) confirmed — "N edge(s) cut" fires
on field 151 (2 edges) and 155 (7 edges; the dry run verified those 7
cuts equal the field's scripted IDLCK set exactly). (a)/(c) not
explicitly retested; no walk-into-counter reports this session.

[TIMER] Stale-timer suppression on fresh-launch load — PLAY-CONFIRMED 2026-07-22; unarmed-tick diagnostic added v2.30.14
------------------------------------------------------------------------
Player bonus test: replayed the No.1 Reactor escape, saved on the
Sector 8 street fields after it (Slot 3 — carries the invisible
still-ticking leftover timer per the v2.30.8 finding), then loaded
that save from a FRESH LAUNCH. Result: total silence — no spurious
"Timer started", T correctly reports no active timer. That is the
v2.30.8 fix confirmed in its strongest form (cold-start load of a
ticking-stale save). Log caveat found while verifying: the unarmed
TimerThread branch logged NOTHING, leaving "no timer in the save" vs
"ticking value correctly suppressed" distinguishable only by
field-trail inference. v2.30.14 adds a once-per-run diagnostic line
("TIMER ticking value N suppressed...") when a sane decrementing value
is being gated — no behavior/speech change.
RESIDUAL (unchanged, likely moot for No.1): a save made DURING a
countdown, loaded fresh, would also be suppressed (STTIM presumably
does not re-fire on load — still unproven either way). The No.1 escape
route has no save point, so this needs a LATER timed sequence that
allows saving; when one is reached, the new diagnostic line plus a
play report will settle it. Research §8 v2.30.8/.14.

[DIALOG] Some short "flash text" screens still speak as one block, not one per page — CONFIRMED by screenshots 2026-07-21, root cause NOT YET FOUND
------------------------------------------------------------------------
Player supplied Screenshots/Dialogs/reactor_scene/ (rs_1..rs_12, each one
button press). rs_6 ("Watch out!") and rs_7 ("This isn't just a
reactor!!") are two SEPARATE on-screen pages (confirmed: distinct
screenshots, no border/name tag, centered — a different window style
than the named dialogue boxes in rs_1-4/rs_9-12) but the mod spoke both
lines together as one utterance on rs_6, going silent on rs_7.
DecodeMessagePages (v2.30.7, ff7_text.cpp) groups decoded lines into
pages of up to kLinesPerPage=4, closing early only on an explicit
page-break byte (0xE8/0xE9) or when the 4-line cap is hit. Two 1-line
pages (2 lines total) never reach that cap, so if there's no explicit
break byte between them, they get merged into one page — exactly this
symptom. kLinesPerPage=4 was always flagged as "FF7's DOCUMENTED
capacity, an ASSUMPTION not read live" (see DecodeMessagePages' own doc
comment) — this screenshot pair is the first concrete proof it's wrong
for at least this window style, which appears to page after a single
line, not four.
NOT FIXED YET — two competing explanations, can't tell which without a
raw-byte log: (a) there IS an explicit 0xE8/0xE9 between the two lines
and something is failing to detect/honor it for this specific window, or
(b) there's no explicit break at all and this window's true on-screen
capacity is smaller than 4 lines (varies by window, not a global
constant). Guessing at a new global kLinesPerPage risks regressing the
dialogues that already work correctly at 4. NEXT: reproduce this exact
reactor-intro moment with debug_log=true — the `MSG raw(...)` hex dump
(log_raw_bytes, already logs the full text) will show whether 0xE8/0xE9
sits between "Watch out!" and "This isn't just a reactor!!"; if it does,
the bug is in how DecodeMessagePages/hook_message reads it; if it
doesn't, the fix needs to read this window's real capacity live rather
than assume 4 for every window.

[NAV] Train-graveyard triggers all say "line N" — DATA LIMITATION (not a bug)
------------------------------------------------------------------------
The trigger entities there are literally dev-named 'line' (log
confirmed; other fields use 'border'/'save point'), so they translate
to "line" + ordinals. No descriptive name exists in the field data.
The pathfinder's direction/distance per trigger is the distinguisher.
Possible future nicety: describe generic 'line' triggers by z-level or
relative position instead of a bare ordinal.

[NAV] Wall + proximity tones dead during timed escape — FIXED v2.30.2 (2026-07-19)
------------------------------------------------------------------------
Player report: neither tone worked during the reactor escape. Log
(debug_log on) pinned it: both gate on "no dialog activity <250-500ms"
(LastDialogActivityTick), and hook_message stamped it UNCONDITIONALLY
every call. That hook is the message-window update loop, which runs
every frame while the countdown-CLOCK window (WSPCL) is on screen —
so the dialog tick stayed fresh the whole escape (MSG heartbeat ~30/sec,
zero dialog transitions). FIX: stamp only when the window holds real
message text (is_valid_dialog_rawptr) + for OOB banked dialogs; the
numeric clock fails that. Throttled "MSG steady win/state/text"
diagnostic added. Deployed both installs. VERIFY at the next timed
sequence: wall + proximity tones work while the clock counts.
(Also confirmed in the same log: v2.34.1 Shift+T freeze WORKS.)

[TIMER] Countdown announce CONFIRMED; freeze fixed v2.34.1 (2026-07-19)
------------------------------------------------------------------------
FIRST ESCAPE RESULT: address 0xDC08BC CONFIRMED (plain T reads time
correctly; announcements work). Shift+T freeze did NOTHING — cause was
the 250ms poll losing the T edge for the faster-releasing two-key
combo (Shift+J/L work at FieldNav's 50ms for this user). v2.34.1:
TimerThread → 50ms poll; freeze now pins the ms counter (0xDC08C0) too
so the clock truly stops (was creeping); per-T-press debug line added.
Deployed both installs 2026-07-19. RE-TEST: Shift+T at the NEXT timed
sequence (No.1 reactor escape is one-shot). Then set debug_log=false
in both cfgs.

[TIMER] (superseded) Countdown announcements + freeze — SHIPPED v2.34 (2026-07-18)
------------------------------------------------------------------------
Shipped BEFORE the first timer is reachable (scorpion one-shot):
STTIM handler disasm proved value+units statically (u32 SECONDS at
0xDC08BC = savemap+0xB84 = FFNx countdown_timer; h*3600+m*60+s).
Announces start / minute boundaries / 30s / final-10 countdown /
"Time is up"; T = on-demand readout; Shift+T = freeze (first gameplay
memory WRITE — value rewritten per poll; display renders from it and
scripts read it, so freeze is game-over-proof). Research §8 v2.34.
VERIFY ON THE ESCAPE RUN (debug_log=true in both installed cfgs):
  - announcements fire at the right marks; tick cadence log lines
    ("TIMER tick N -> N-1 (Xms)") should show ~1000ms;
  - whether the clock pauses in menus/battles (log start/stop lines);
  - T readout matches the on-screen clock; Shift+T freezes the
    visible clock and resumes cleanly.
  - AFTER the run: set debug_log=false in both installed cfgs.
  - If announcements never fire, grab the log — the TIMER lines will
    show whether the address moved at all.

[MENU] Status screen TTS — v2.33 PLAY-CONFIRMED 2026-07-18
------------------------------------------------------------------------
Full stat sheet on entry: name/level/HP/MP/EXP+next/limit, six
EFFECTIVE stats + Attack/Defense/Magic atk/Magic def (from the shared
char-data block 0xDBA498 — the "battle" block is menu-populated too,
found by a single-hit BSS pattern scan against the screenshot),
equipment names. Gate = dispatch index 5 (FFNx's own status_menu_sub
name); character = CHARSEL_CHOSEN. Staleness guard: block HP pair must
match savemap record, else base stats only. Research §8 v2.33.
RESIDUALS:
  - Attack%/Defense%/Magic def% not spoken (drawn from kernel
    weapon/armor data records at render time, nowhere in memory —
    needs kernel equipment-data parsing; def% alone = dex/4 if a
    partial ever seems worthwhile).
  - Status pages 2/3 (elemental/added-effect tables): unmapped,
    silent. Needs a page-indicator find if wanted.
  - PLAY-CONFIRMED 2026-07-18: "Status menu works as expected."

[MENU] Order menu TTS — v2.32 PLAY-CONFIRMED 2026-07-18 ("It works perfectly.")
------------------------------------------------------------------------
User request: position + row when a member is selected, spoken how-to
for changing order. KEY INSIGHT (user's ear): Order plays NO chime — it
is not a screen at all; the main menu moves focus into the party pane
(dispatch index stays 0). Guided scan found cursor 0xDC11C4 / latch
0xDC1320 / row byte = char record +0x20 (community's +0x1F is one byte
off); the 0x6CA346 handler disasm found the rest: MENU_FOCUS_MODE
0xDC1324 (0 bar / 1 char-select+chime / 2 Order, silent),
MENU_DISABLED_ROWS 0xDC1130, char-select cursor 0xDC118C (pre-solves
Magic/Equip/Status entry). Speaks: entry how-to + member; "Name,
position N, front/back row"; "Name selected. Confirm another member to
swap, or Name again to change rows."; outcomes read from data ("Swapped.
<new order>" / "Name, back row" / "Cancelled"); mode-1 "Choose a
member." + names; main-menu rows append ", not available" when grayed
(the user's Materia observation, now with parity). Research §8 v2.32.
PLAY-CONFIRMED 2026-07-18 same day: "It works perfectly." — the
static-derived focus-mode semantics (0/1/2) are thereby live-validated.

[MENU] Item menu TTS — v2.31 PLAY-CONFIRMED 2026-07-18
------------------------------------------------------------------------
User request with screenshots; shipped and PLAY-CONFIRMED the same day
("everything we can test works"): list rows with counts + descriptions,
Empty rows, Use/Arrange/Key Items top bar, target pane with name +
HP/MP, "N left" on use. No spurious "Item menu" on the plain main menu
reported — the MENU_DISPATCH_INDEX==1 gate holds in practice (its
plain-main-menu value still never directly sampled; note kept in §4).
Research doc §8 v2.31 has the derivation; §4/§14 the addresses.
RESIDUALS not yet testable (story/state-gated):
  - List cursor window-vs-absolute: 3 items can't scroll (the v2.29.2
    lesson). Once inventory > visible rows, re-verify; if rows past
    the window misannounce, scan the nearby scroll word (save menu
    precedent: cursor+~0x10).
  - Arrange popup and Key Items pane: ITEMMENU_MODE values unmapped —
    v2.31 stays SILENT there and debug-logs "ITEMMENU unmapped
    mode=N"; a play report plus that log line maps them.
  - Equipment rows speak name+quantity but no description (weapon/
    armor/accessory description sections not yet signature-scanned).
  - Key-item names: kernel section not yet located (no key items to
    ground a signature against). Needed for the Key Items pane.


[MENU] Save / Continue menu TTS — COMPLETE (v2.29-v2.29.5, 2026-07-17)
------------------------------------------------------------------------
User request with screenshots (Screenshots/Menus/): make the save-point
SAVE menu and the title-screen CONTINUE (load) menu speak. Both share
the same two screens: a 10-entry file grid ("Save 1".."Save 10"), then
a 15-slot list per file (preview or EMPTY); only the header verb
differs, and mode needs no scan — save is only reachable in-field,
load only from the title (context the mod already tracks).

DONE so far:
  - Slot preview data source SOLVED WITHOUT MEMORY SCANNING: previews
    live in workingdir\save\saveNN.ff7; layout DERIVED from the
    player's real save00.ff7 against screenshot ground truth
    (ff7_savefile_preview_derive.py; research doc §5 table:
    slot = 9+n*0x10F4, +0x04 level, +0x05 portraits, +0x08 name,
    +0x20 gil, +0x24 seconds, +0x28 location, empty = all zero).
  - Scan script READY: investigate/ff7_save_menu_scan.py (spoken
    guidance; press-and-revert rounds immune to grid wrap + A/B/A
    phase toggle + live speak-back verify pass). Finds: grid cursor,
    grid<->slot-list phase, slot cursor.
SHIPPED v2.29 (2026-07-17): scan run by user (grid cursor
live-verified in-session; single candidates for both cursors),
SAVEMENU_* wired, SaveMenuThread speaking grid/slot/pane from disk
previews. Deployed ~12:05. Residuals:
  - PENDING PLAY-TEST: grid/slot announces at the save point; the
    save-confirm flow.
  - ~~Title Continue menu expected on same addresses~~ LIVE-DISPROVED
    then RESOLVED same day (v2.29.1): Continue menu has its own state
    in the title block — LOADMENU_* 0xDD6D98/0xDD6DD4/0xDD7700 (grid
    cursor live-verified by scan speak-back; +0x3C struct echo).
    SaveMenuThread is mode-aware (save = frozen-row gate;
    load = FIELD_ID==0). Deployed ~12:20.
  - v2.29.2 (same day, two play reports): slot cursors are the
    3-row WINDOW index — absolute slot = row + scroll (title scroll
    0xDD6DE4 live-confirmed by ff7_slot_scroll_probe.py; save-side
    0xDC6B2C inferred at the same +0x10). SAVEMENU_PHASE 0xDC1210
    DISPROVED (oscillates -> endless alternating announcements);
    pane for both menus = LOADMENU_LIST_PTR nonzero. Deployed ~12:45.
    Slot count to 15 PLAY-CONFIRMED in the Continue menu 2026-07-17.
  - v2.29.3/.4 (same day, play reports: grid second row misannounced,
    then rows swapped): grid cursor is the COLUMN 0..4; row byte at
    grid+4, 0=top 1=bottom (v2.29.3 shipped it inverted — probe
    baseline misread because the player started the probe parked on
    the bottom row; play report settled polarity). file = row*5+col.
    Deployed ~17:40. PLAY-CONFIRMED 2026-07-17 evening: both grid
    rows correct in both menus, deep slot verified end-to-end
    ("Save 6 slot 15" matched the game's own GAME 15 header), and
    the field probe run 20260717_200802 LIVE-CONFIRMED the save-side
    grid-row 0xDC6AE4 and slot-scroll 0xDC6B2C (no inferred
    addresses remain).
  - ~~Save-confirm dialog not spoken~~ RESOLVED v2.29.5 (2026-07-17):
    scan found SAVEMENU_WIDGET_STATE 0xDCA028 (1=slot list,
    7=confirm dialog; the menu's own mode variable) and
    SAVEMENU_CONFIRM_CURSOR 0xDC6C6C (0=Yes 1=No), both single
    candidates + speak-back verified. Announce on open + Yes/No on
    toggle. Deployed ~20:30. PLAY-CONFIRMED same evening
    ("Everything works"), including a real save through Yes.
    ~~Overwrite watch~~ CLOSED: player overwrote an existing save —
    the game shows NO second dialog; the one confirm covers both
    cases. Save/continue menu feature complete (v2.29-v2.29.5).
  - World-map save menu: currently silent (FIELD_ID==0 there routes
    to the load branch's title-block addresses). Fix needs a
    world-map GAME_MODE value sampled live, or a positive save-menu
    open flag. Low priority until the player reaches the world map.
  - First grid screen is silent until the first cursor move
    (no "menu opened" flag known — same main-menu limitation).
    If a flag is ever wanted: scan main-menu -> save-menu entry.


[TTS] Controller-button glyph bytes in dialog text — PARTIALLY HANDLED, byte-value claim below is STALE
------------------------------------------------------
STALE CLAIM (pre-dates the ff7tk-sourced kExtendedChars rewrite, no longer
matches code): this entry used to claim 0xD6 decodes as "Û" and 0xD9 as
"Ù" and are the unhandled button-icon bytes. Checked against the CURRENT
table (ff7_text.cpp kExtendedChars, indexed byte-0x5F): 0xD6 and 0xD9 are
now both empty/reserved slots (L'\0', already silently discarded as a
guarded space — no garbage spoken). The real Û/Ù mappings currently live
at 0x85→Û / 0x84→Ù (and again at 0xD3→Û / 0xD2→Ò... actually 0xD2→Ù,
0xD1→Ò — see the table's 0xD0/0xD8 rows). So the ORIGINAL bug this entry
describes (button icons somehow rendering as accented Latin garbage) is
not reproducible against current code with the byte values as given.

ALSO NOTE: real FF7 controller-button icon bytes ARE already identified
and handled — 0xF6-0xF9 (Circle/Triangle/Square/Cross, sourced from the
ff7tk eng[] table) are consumed as single bytes and silently discarded in
decode_walk (ff7_text.cpp), added since this TODO entry was first written.
They're silent, not descriptive, so the original "fix idea" (map to a
short string like "[Cancel]"/"[Dpad]") is still worth doing — it just
needs to target 0xF6-0xF9, not 0xD6/0xD9.

NEXT: if a player encounters a dialog that visibly shows a button-icon
sprite (Circle/Triangle/Square/Cross/D-pad) and the mod reads it as
silence or garbage, capture a fresh raw byte dump (debug_log=true,
log_raw_bytes already dumps up to 256 bytes) through that exact dialog to
re-derive the correct byte value before mapping it — do not guess from
this entry's original byte claims again.


[TTS] "New Game" spoken during company logo splash (~350ms after launch)
------------------------------------------------------------------------
TITLE_CURSOR (0x00DD6F24) is in BSS and is zero-initialized before the title
screen module runs. TitleCursorThread's first poll fires at ~350ms while
Eidos/Square Enix logos play; it reads 0 (= New Game) and announces it.

The FIELD_ID gate (added in v1.9.1) prevents false positives during field
gameplay, but cannot distinguish "splash screen" from "title screen" because
FIELD_ID is 0 in both cases.

After the spurious splash announce, last_cursor = 0. The real title screen
also starts at cursor = 0, so no second announce fires — user must navigate
once (Up/Down) to hear their position.

Fix ideas:
  - Find a reliable in-process "title screen is actually active" indicator
    (separate from FIELD_ID). Possible candidates: a title-screen state
    variable, a menu-system flag, or the presence of specific title-screen
    assets in memory.
  - Alternatively, delay TitleCursorThread's first poll until after the splash
    duration (e.g., 5s) — crude but effective if splash length is fixed.
  - Document as known limitation until a reliable indicator is found.


[TTS] Quit dialog — speak prompt text on every open
----------------------------------------------------
Currently the "Do you want to quit?" dialog speaks only the cursor position
(Yes/No) when it first appears, with no context for what the dialog is asking.
A blind player who re-opens the dialog after cancelling hears only "No" with no
indication of what question is being answered.

Fix: detect the dialog-open transition and prepend a short spoken prompt, e.g.
"Quit game?" before the cursor position, every time the dialog appears.
Requires a reliable QUIT_OPEN flag — current candidate 0x00DC0FB1 does not
return to 0 after the dialog is cancelled (see ff7_addresses.h). A new scan
comparing "main menu without dialog" → "dialog open" → "dialog dismissed to
main menu" (a 3-way scan, not 2-way like the original) would isolate the flag.


[TTS] Quit dialog — re-announce selected option on every dialog open
---------------------------------------------------------------------
When the Quit dialog is opened a second time (e.g., player pressed No then
opens Quit again), the cursor defaults to No, but QUIT_CURSOR is already at 1
(No) from the last session so last_quit_cursor matches and no announce fires.
Player hears silence and must press Left then Right to hear "No".

Fix: detect dialog re-open (requires QUIT_OPEN flag above, or detect
MENU_CURSOR transitioning to 10 as a proxy) and reset last_quit_cursor to 0xFF
at that moment so the default position is always announced.


[NAV] Wall-slide tone (angled wall contact gives no feedback)
--------------------------------------------------------------
The v2.6 wall-bump tone only fires on a DEAD STOP (direction held, position
frozen). Hitting a wall at an angle stays silent because FF7's walkmesh
redirects the player's FULL velocity along the wall — measured 2026-07-09
(ff7_wall_slide_verify.py): sliding displacement per poll was IDENTICAL to
free walking (median 32765 units/50ms in both; ratio exactly 1.00). So no
displacement-magnitude threshold can ever detect sliding — the engine gives
away no speed.

What WOULD work: compare the COMMANDED direction (current_key_input_status
direction bits) against the ACTUAL motion direction (atan2 of per-poll
displacement). Free movement: they agree. Sliding: they diverge strongly.
Complication: input is camera-relative, world position is world-absolute,
and every field has its own camera orientation — mapping input to expected
world direction needs either the field camera matrix (field file section 2)
or empirical per-field calibration (learn the input→motion mapping while
moving freely, flag sustained divergence as sliding).

Useful measured constants (same session):
  - displacement per 50ms poll = movement_speed * 32 / 20Hz-tick, exactly
    proportional (walk speed=1024 → 32765/poll, run=4096 → 131062/poll)
  - sliding occasionally hard-stops for 1-2 polls (p10 displacement was 0
    in the slide phase) — those moments already trigger the existing
    dead-stop tone, which is why angled contact sometimes chirps.

User decision 2026-07-09: wanted as a DISTINCT tone (higher/softer than the
220 Hz dead-stop tone) so "fully blocked" and "scraping along a wall" are
distinguishable by ear. Deferred until the direction-mismatch approach is
designed.


[TTS] Name entry — side panel: RESOLVED 2026-07-12 (v2.8.2), one residual
------------------------------------------------------------------------
The panel is fully wired: NAME_ENTRY_PANE_FLAG (0x921ED4, 0=grid 1=panel,
found by A/B/A revert scan + live-verified) gates everything, and
NAME_ENTRY_PANEL_INDEX (0xDD4574) names the button (0=Space 1=Delete
2=Select 3=Default; 0/1 proven by buffer effects via
ff7_name_entry_panel_probe.py).

RESIDUAL: Select(2)/Default(3) order is from the on-screen layout plus the
player's ear-confirmation of ff7_name_entry_pane_verify.py — not yet proven
by effect (Default = name rewrite, Select = screen close). The player's
normal use will prove it immediately: if confirming "Select" ever restores
the name instead of closing the screen, swap indices 2/3 in kPanelNames
(proxy.cpp) and in ff7_addresses.h.

Also still unresolved (harmless): 0xDD45E8 (u32, small values /
0xFFFFFFFF, per-screen reset — never changed during the panel probe).

Also unresolved from the same session: 0xDD46F0 (8 on Cloud screen with
"CloudGHR", 6 on Barret screen with "Barret" — maybe name length at screen
open; not updated live during editing).


[TTS] Name entry — grid row 5 columns 8-9 unverified
-----------------------------------------------------
kNameGrid guesses apostrophe (col 8) and double-quote (col 9) for row 5;
the reference screenshot was unreadable there. If a player lands on those
cells the spoken guess may be wrong. The add-echo is immune (it decodes the
byte the game wrote), so navigating there and pressing Confirm reveals the
truth — fix kNameGrid in proxy.cpp when observed.


[TTS] Party member KO announcements — v2.30 PLAY-CONFIRMED 2026-07-18
--------------------------------------------------------------------------------------
Requested 2026-07-13, shipped v2.30 once the party had a second member:
BattleActionThread's liveness watcher extended to party slots 0-2,
three-state Unseen/Alive/Dead tracking (battle-start KO recorded
silently, so a later revival still announces), "X is down" / "X is
back up" via PartySlotLabel, shared v2.12.2 quiet-gap delivery.
PLAY-CONFIRMED 2026-07-18: "The party member down and back up message
works." Research doc §8 v2.30.

[NAV] Wall-bump tone looped through the game-over screen — FIXED v2.30.1, awaiting play-test
---------------------------------------------------------------------------------------------
Play report 2026-07-18: after a party wipe the 220 Hz tone repeated
until the New Game/Continue screen. Root cause: game-over is the Gate-2
stale-input scenario (input status frozen at the direction held when
the fatal battle triggered, position frozen) but GAME_MODE evidently
reads as field (0) there, so no gate closes. Fix v2.30.1: tone must be
ARMED by real observed movement (two differing valid position samples)
in the current gate-open episode; any gate closing disarms. Frozen
modules can't fake walking, so no stale-input state can loop the tone.
Accepted cost: battle ending flush against a wall with direction never
released waits for one step before the first tone. Debug log line
"WALL tone suppressed" marks swallowed episodes. Deployed both
installs 2026-07-18. PENDING PLAY-TEST: (1) normal wall bumps
unchanged, (2) party wipe now silent through game over.

[NAV] v2.22 turn-by-turn: CONFIRMED in play (2026-07-16); v2.23 additions pending
----------------------------------------------------------------------------------
v2.22 route quality PLAY-CONFIRMED: user reports multi-turn routes with
"turns land at the walls" (session log full of clean multi-leg routes).
v2.23 play-test status (user report 2026-07-16):
  - "Screen: <name>" announcements CONFIRMED ("screen changes work good");
    user now wants friendly names — see the raised-priority item below.
  - Turn-by-turn incl. "up and left" diagonals: CONFIRMED fine.
  - v2.22.1 battle-text fix: no garbage seen since — holding.
  - JOURNEY planning (ladder sequences): still awaiting a cross-level
    directions request in play. If a wrong connector is ever named, the
    XY pair threshold (JOURNEY_PAIR_DIST=300, measured 134) or a
    name-based filter (ladder/slide words only) are the knobs.
If routes feel choppy (too many tiny "then" segments), tune kMinSegLen /
kMaxSpoken in RouteToSpeech — the funnel itself is dry-run-verified.

[NAV] FF4-scheme keys deferred until their prerequisites exist
----------------------------------------------------------------
accessiblity_keys.txt (repo root) is the binding source of truth (parity
with the FF1-6 accessibility mods — see memory feedback_key_parity). The
v2.14 pathfinder implements the core browser (J/L/[/], Shift+J/L/-/=, K,
Shift+K, \, P, M). Still unmapped, waiting on features:

  - Shift+\ / Shift+P: toggle "valid path only" filter — UNBLOCKED by
    v2.22 (walkmesh pathfinding exists; A* reachability per destination
    is a cheap byproduct of the route code). Not yet wired to a key.
  - Ctrl+\: layer filter — walkmesh triangle awareness now exists
    (v2.22); still needs a notion of "layer" (connected component of the
    triangle graph is probably the right definition). Not yet wired.
  - Ctrl+Arrows: teleport one tile — needs a safe position-write method
    (writing model_pos highres coords + walkmesh triangle id).
  - Shift+M: map exit filter toggle.
  - Announcements section: G (gil), H (battle HP readout — party HP is
    already spoken during targeting; H would be on-demand), I (setting/
    item descriptions), V (vehicle), T (timers).
  - Audio section: ' (footsteps toggle), ; (wall tones toggle — the
    wall-bump tone exists but is config-file-only today), 9 (audio
    beacons), F8 (mod menu).
  - Waypoints section: , . / Shift+/ Ctrl+/ etc. — needs a waypoint
    store (per-field named positions saved to disk).
  - Game Keys F1 (walk/run toggle) and F3 (random encounter toggle) are
    game-altering features, not announcements — decide separately.

[NAV] Pathfinder lists exits that are not open yet
----------------------------------------------------
Player observation 2026-07-13 (accepted for v1): the destination browser
tracked an exit that was not yet usable (story-locked). Gateways in the
triggers header exist whether or not the field script currently allows
passage. Possible detection routes when this gets annoying:
  - the four unknown bytes at field_gateway +0x14 may hold an
    enabled/disabled flag — diff them on a field with a locked exit
    before/after it unlocks;
  - the show_arrow_flag[12] array (header +0x1B8, per FFNx ff7.h) likely
    mirrors "the game draws this exit's arrow" — sighted players see
    arrows only on usable exits, so gating on it would also restore
    information parity.

[NAV] People naming: RESOLVED v2.16 (model-loader labels); residuals
----------------------------------------------------------------------
DONE 2026-07-13: people are named from the model loader section's
descriptive .char names ("shinra hei", "main ballet"), duplicates get
slot-order ordinals. PLAY-TEST CONFIRMED 2026-07-14 (names correct
in-game). Residuals:
  - ~~Save-point detection is a HEURISTIC~~ CONFIRMED game-wide 2026-07-14
    by the flevel catalog: "fieldbg saveicn" is the only "save" label in
    all 720 fields.
  - ~~Labels are developer romaji~~ RESOLVED v2.20 (2026-07-15):
    TranslateDevLabel (proxy.cpp) translates word-by-word from a table
    built on the complete v2.18 model catalog; character words resolve to
    live savemap names via the v2.19 machinery. Unknown words pass
    through unchanged BY DESIGN (wrong > terse); extend the table from
    play reports / the catalog logs. WATCH: the AVALANCHE trio mapping
    (avaman/avawoman/avafat -> Biggs/Jessie/Wedge) is correct on the
    bombing-mission fields (entity 'av j/b/w' evidence) but those .char
    models also appear on cargoin/blackbg1 — if play there speaks a
    wrong trio name, demote the three entries to "man"/"woman"/"big man".
  - The old character_id (+0x6C) route stays DISPROVED — never name from it.

[NAV] Items category: chest open/closed — RESOLVED v2.18.1 (2026-07-14), TIGHTENED v2.18.2
---------------------------------------------------------------------------
Floor pickups despawn when collected (off-mesh filter drops them). Chests:
"Chest, opened" suffix, live-derived at the nmkin_1 metal chest, survives
field re-entry (init script re-poses looted chests open). v2.18.1 shipped
a single-condition check (lastFrame (+0x6A) != 0); v2.18.2's code-review
pass tightened it to a TWO-condition check — lastFrame != 0 AND
currentFrame == lastFrame<<4 (the lid animation held at its own final
subframe) — to stop a non-lid animation that RETURNS to rest (deny
rattle, cutscene pose) from misreading as "opened"; the residual risk
flips to a missed "opened" instead, the safer direction (a wasted walk
vs. skipped treasure). Full state matrix in research §8 v2.18.1/.2.
Residual watch items:
  - Confirmed on the METAL variant only; wood ("trb wood") and glow
    ("trb glow") variants expected identical — verify from normal play
    when one is encountered (wrong announce = report immediately).
  - "fieldbg taru" (barrels, chrin_2) excluded from Items — no evidence
    they give items; revisit if play shows otherwise.
  - Optional: config key to HIDE opened chests instead of suffixing,
    if the player prefers, once they've lived with the announce.

[NAV] Triggers category (v2.17) residuals
-------------------------------------------
Shipped 2026-07-14: LINE trigger zones as the 5th category, named by
owning entity dev name ('evb', 'drE'). Possible refinements from play:
  - The +0x0E state latch may mean "player currently inside the zone" —
    if confirmed by observation it could announce "you are on the
    trigger" instead of a distance.
  - ~~Entity dev names are terse~~ RESOLVED v2.20 (2026-07-15): entity
    names now run through the same translation table
    (TranslateEntityName), grounded in the new game-wide entity-name
    catalog (investigate/ff7_flevel_entity_names_catalog.py — 2412
    distinct names; "dr" IS door, but ladders are "lad/ladd/ladu", not
    "hasigo"; "kaidan" does not occur). WATCH: "ladd"/"ladu" are spoken
    as "ladder down"/"ladder up" (both ends of a ladder observed on
    bonevil/nmkin_2) — if play shows the suffix means something else,
    fold both to plain "ladder".
  - ~~Two/three-letter stems still spoken raw ("ev","dr","jp")~~
    RESOLVED v2.28 (2026-07-17, user play report): second table pass
    grounded per-field (investigate/ff7_short_entity_context.py — full
    entity list of every field each stem occurs in). dr=door, jp=jump
    (+ujp/mjp/sjp/jpj/jpr), ev=event, ldu/ldd=ladder up/down,
    esc=escalator, elevator family, mes/meskun=message,
    checkun/chekun=check, ln/lin=line, party shorthands
    cl/ti/ba/ea/re/rd/yuf/ket/vin -> live names. STILL RAW BY DESIGN
    (no evidence): "sp" (jail2+rcktin6 only; NOT the save point —
    jail2 already has save/savel0), "jl", "se", "lg", "mat", "mate",
    and the ad/dir/dic/drctr film-crew controllers. If play surfaces
    one of these, note WHICH FIELD — the context catalog turns a field
    sighting into a translation.
  - Trigger lines whose script does nothing until a story flag are still
    listed while enabled — same accepted limitation as story-locked
    exits.

[NAV] Friendly map names: RESOLVED v2.24 (2026-07-16)
-------------------------------------------------------
DONE in one evening session (research doc §8 v2.24): the caption lives at
LOCATION_NAME_BUFFER 0xDC0C44 (savemap+0xF0C), written by the MPNAM
opcode's storage callee — found statically (ff7_mpnam_static.py) after
the live scan DISPROVED the savemap-preview theory (preview block is
save-time-only), then live-confirmed the same evening ("Sector 1
Station" ↔ "Platform" tracking real screen changes). v2.24 speaks it:
screen announce = caption only, M = caption + internal code. Residuals:
  - Play-test on a brand-new game: buffer should be blank until the
    first MPNAM → internal-name fallback covers it; confirm no garbage.
  - LESSON APPLIED: investigation scripts now speak via PowerShell
    System.Speech subprocess (pywin32 absent on system python made the
    first scan silently mute for a blind user).

[CONFIG] Auto-create default config file if missing
----------------------------------------------------
If the user places version.dll in the FF7 folder but forgets ffvii_accessibility.cfg,
Config::Load() currently fails silently and the mod runs with all defaults
(which is actually correct behavior). However, there is no config file for the
user to edit to enable/disable features.

Fix: in Config::Load(), after failing to open the file, write a default
ffvii_accessibility.cfg to the same directory with all defaults and a header
comment explaining each option. This gives the user an editable starting point
without requiring them to find documentation.

Implementation note: the DLL knows its own path via GetModuleHandleEx +
GetModuleFileNameA (same pattern used in TTS::Init() to locate Tolk.dll).
